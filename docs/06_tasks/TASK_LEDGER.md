# Task Ledger

Track only current, blocked, and recent tasks here so Codex does not continue automatically. Older completed rows through T-170 are archived under `docs/09_frozen/task_ledgers/`; active blocked T-157 remains below. Notes should keep conclusions and pointers only; validation detail belongs in `docs/00_memory/WORKLOG.md` or frozen worklogs. Detailed task records T-001 through T-088 and workflow task records remain archived under `docs/09_frozen/task_records_2026-06-26/`.

Current branch and task-numbering baseline: use `codex/pet-fit-structure-cleanup`; use `T-182` for the next non-APNs task unless the user explicitly names another branch or task ID.

Active blocked task: T-157 is waiting on paid Apple Developer Program access before APNs secrets (`APNS_KEY_ID`, `APNS_TEAM_ID`, `APNS_TOPIC`, `APNS_PRIVATE_KEY`) and Edge Function deploy.

## Recent and Current Tasks

| ID | Task | Status | Mode | Milestone | Files/Docs | Checks | Notes |
|---|---|---|---|---|---|---|---|
| T-181 | Current-state stale pointer correction | completed | Quick | G0 | current state, task ledger, worklog, frozen archives | `git diff --check`; context hygiene; commit and push | Corrects stale T-180 next-task and validation-current-state text found during follow-up review, updates the next task to T-182, and rolls T-170/T-173 out of active windows. No app/runtime/backend behavior changed. |
| T-180 | Standing Git auto-push authorization | completed | Quick | G0 | `AGENTS.md`, workflow/Git rules, decision log, memory docs | `git diff --check`; context hygiene; commit and push | Records the user's standing approval for automatic task-completion commits and pushes after required validation passes. Guardrails keep PRs/tags/merge/reset/rebase/non-Git remote writes and unrelated user work outside this approval. |
| T-179 | Active Markdown budget reduction | completed | Quick | G0 | hygiene script, active indexes, decision log, memory docs, frozen external plan, ledger/worklog archives | context hygiene; hygiene tests; `git diff --check` | Lowers active ledger/worklog windows, trims active indexes, tightens decision/structure budgets, archives the adopted watch-items plan, and brings active Markdown below the 85% waterline. No iOS/Supabase/runtime behavior changed. |
| T-178 | Context hygiene v5 path checks | completed | Quick | G0 | context hygiene script/tests, structure log, memory docs, ledger/worklog archives | RED/GREEN hygiene tests; context hygiene with and without `rg`; `git diff --check` | Adds backtick path existence checks, visible checked-path counts, active Markdown 85% waterline warning, and English-only hygiene failures. Fixes two stale removed-template references in the structure log. No iOS/Supabase/runtime behavior changed. |
| T-177 | ROADMAP DoD and ignore cleanup | completed | Quick | G0 | `.rgignore`, `.gitignore`, ROADMAP, memory docs, ledger/worklog archives | `git diff --check`; context hygiene; ignore/root-draft checks | Removes dead governance-plan ignore entries, expands V1.0 DoD into a checklist, reorders ROADMAP candidate rows by R ID, and records root external reports as live drafts while frozen copies remain adopted snapshots. No iOS, Supabase, runtime, simulator, push, or remote writes. |
| T-176 | Entrypoint and branch fact-source alignment | completed | Quick | G0 | `AGENTS.md`, `CLAUDE.md`, decision log, memory docs, ledger/worklog archives | `git diff --check`; context hygiene | Removes hardcoded branch baseline from `AGENTS.md`, points branch fact to CURRENT_STATE, and updates Claude's planning/rule-review map with ROADMAP, Git rules, and DECISION_LOG. No iOS, Supabase, runtime, simulator, push, or remote writes. |
| T-175 | Context hygiene v4 failure-mode checks | completed | Quick | G0 | hygiene script/tests, meta-review template, decision log, memory docs, ledger/worklog archives | RED/GREEN hygiene tests; context hygiene; `git diff --check` | Adds rg-unavailable fallback, fail-closed current fact extraction, meta-review cadence tracking, and task-ledger row-length enforcement. No iOS, Supabase, runtime, simulator, commit, push, or remote writes. |
| T-174 | Decision log prearchive and governance review intake | completed | Quick | G0 | decision log snapshot/index, frozen external review plan, frozen indexes, memory docs, ledger/worklog archives | `git diff --check`; context hygiene; decision snapshot and git-history checks | Implements batch A from the governance review fix plan: records one-time historical commit exception for `01c80e4` and `6d1da33`, snapshots and trims the active decision log, and archives the adopted root review plan. No history rewrite, commit, push, iOS, Supabase, runtime, or simulator change. |
| T-173 | Main governance divergence reconciliation | completed | Quick | G0 | `GITHUB_RULES.md`, decision log, roadmap, memory docs, ledger/worklog archives | read-only git review of `2fddf7b`; `git diff --check`; context hygiene; targeted divergence search | Reviews `main`/`origin/main` commit `2fddf7b` and records it as superseded by this branch's governance architecture. Do not merge it back; future `main` alignment should carry this branch forward or cherry-pick only explicitly reviewed non-stale changes. |
| T-172 | Rule-change process | completed | Quick | G0 | `AGENTS.md`, `CLAUDE.md`, workflow rules, decision log, roadmap, memory docs, ledger/worklog archives | `git diff --check`; context hygiene; targeted rule search | Requires changes to `AGENTS.md`, `CLAUDE.md`, or `docs/05_workflow/**` to be standalone numbered tasks with a decision-log entry and context hygiene. Adds a stop condition for incidental rule changes. No iOS/Supabase/runtime behavior changed. |
| T-171 | Periodic meta-review template | completed | Quick | G0 | `META_REVIEW_TEMPLATE.md`, task/docs indexes, roadmap, memory docs, ledger/worklog archives | `git diff --check`; context hygiene | Adds a compact template requiring a documentation-governance review every 10 completed tasks or weekly, with status, diff, context hygiene, and targeted search checks. No iOS/Supabase/runtime behavior changed. |
| T-157 | Customer APNs push notification foundation | blocked | Deep | M2 | APNs migrations, dispatcher Edge Function, iOS registration, tests, memory docs | Full T-157 validation recorded in frozen worklogs and task records; context hygiene | Remote DB foundation is applied on `lqmasbuqzvcvtawonjlb`, but dispatcher remains undeployed. Blocked until paid Apple Developer access provides `APNS_KEY_ID`, `APNS_TEAM_ID`, `APNS_TOPIC`, and `APNS_PRIVATE_KEY`. |
