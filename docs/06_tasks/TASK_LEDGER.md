# Task Ledger

Track only current, blocked, and recent tasks here so Codex does not continue automatically. Older completed rows through T-190 are archived under `docs/09_frozen/task_ledgers/`; active blocked T-157 remains below. Notes should keep conclusions and pointers only; validation detail belongs in `docs/00_memory/WORKLOG.md` or frozen worklogs. Detailed task records T-001 through T-088 and workflow task records remain archived under `docs/09_frozen/task_records_2026-06-26/`.

Current branch and task-numbering baseline: use `codex/pet-fit-structure-cleanup`; use `T-203` for the next non-APNs task unless the user explicitly names another branch or task ID.

Active blocked task: T-157 is waiting on paid Apple Developer Program access before APNs secrets (`APNS_KEY_ID`, `APNS_TEAM_ID`, `APNS_TOPIC`, `APNS_PRIVATE_KEY`) and Edge Function deploy.

## Recent and Current Tasks

| ID | Task | Status | Mode | Milestone | Files/Docs | Checks | Notes |
|---|---|---|---|---|---|---|---|
| T-202 | V1.0 ideal-operation task series | completed | Quick | M6-M8/D | roadmap, execution queue, decision log, memory docs | `git diff --check`; context hygiene; commit and push | Adopts root V1.0 review draft into governed Roadmap/Queue without preassigning future T IDs. Active queue starts at Q-16; Q-90...Q-92 stay blocked on external credentials/authorization. |
| T-201 | Release readiness dry run | completed | Deep | M5/R-015 | release evidence, App Store privacy pointer, Supabase check script, roadmap/memory docs | Preflight; TestOps unit; privacy test; TestOps doctor/smoke5/matching dry-runs; TestOps launch smoke; Supabase check; security/performance advisors; full iOS test/build; `git diff --check`; context hygiene; commit and push | Closes Q-15 for local/read-only release readiness. Security advisor shows only the known Auth leaked-password WARN, performance advisor has no issues, and no remote write/release upload/App Store Connect change/migration/seed/deploy/tag/PR occurred. Q-07 and Q-09 remain externally blocked. |
| T-200 | Notification ordering test expansion | completed | Standard | M4/R-014 | customer notification store tests, roadmap/memory docs | CustomerNotificationsStoreTests RED/GREEN; full `./scripts/ios-test.sh`; `./scripts/ios-build.sh`; `git diff --check`; context hygiene; commit and push | Closes Q-14 by making same-timestamp customer notification ordering deterministic in focused Store tests, replacing the flaky mark-all read assertion, and restoring the full iOS test gate to passing. No production code, Supabase schema, migration, remote write, dependency, or UI behavior changed. |
| T-199 | Private image network resilience | completed | Standard | M3/R-013 | shared private image loader, private image tests, roadmap/memory docs | PrivateImageLoaderTests RED/GREEN; `./scripts/ios-build.sh`; `git diff --check`; context hygiene; commit and push | Closes Q-13 for the shared image path by retrying transient private-image download failures once, preferring fresh data over stale cache after a transient refresh failure, and refusing cached fallback when a refresh is cancelled. No Supabase schema, migration, storage policy, or remote write changed. |
| T-198 | Accessibility and copy audit | completed | Standard | M3/R-012 | design tokens, primary action styling, booking copy, customer pet card accessibility, accessibility tests, roadmap/memory docs | Focused booking/customer pet/design-token tests; `./scripts/ios-build.sh`; `git diff --check`; context hygiene; commit and push | Closes Q-12 by adding contrast-checked semantic color constants, dark foreground primary buttons, role-specific bookings/schedule load failure copy, and pet-card VoiceOver summaries. Runtime VoiceOver pass was limited to code/test/build validation because XcodeBuildMCP UI tools were unavailable in this session. |
| T-197 | Local operational crash and funnel evidence | completed | Standard | M3/R-011 | operational event recorder, app launch/auth/role instrumentation, debug/privacy docs, roadmap/memory docs | Focused `AppOperationalEventTests`; `./scripts/ios-build.sh`; `git diff --check`; context hygiene; commit and push | Closes Q-11 by adding sanitized local-only lifecycle/funnel evidence for launch, foreground/background, auth state, role resolution, profile-load failure, and suspected prior-run interruption. No third-party SDK, network analytics, crash-report upload, Supabase schema, migration, or remote write changed. |
| T-195 | Privacy and Support URLs | completed | Standard | M3/R-010 | release privacy/support pages, release link constants, account legal links, privacy tests, roadmap/memory docs | App Store privacy Node test RED/GREEN; `./scripts/ios-build.sh`; link checks; `git diff --check`; context hygiene; commit and push | Closes Q-10 by replacing App Store URL blockers with public HTTPS GitHub release pages, exposing Privacy Policy and Support links from customer/groomer/generic Account surfaces, and aligning roadmap status. App Store Connect metadata itself was not changed because that is a remote product setting. |
| T-194 | Realtime foreground chat | completed | Standard | M2/R-008 | chat repository/store/view, debug repository wrapper, chat tests, memory docs | Supabase changelog/docs review; focused ChatStoreTests; `./scripts/ios-build.sh`; `git diff --check`; context hygiene; commit and push | Closes Q-08 by adding a foreground message INSERT stream through the ChatRepository boundary, thread lifecycle subscribe/unsubscribe, foreground refresh for message list/thread, Debug Console events, and subscription failure handling that logs without showing user-facing toast. Two-role live smoke was not run because it requires unauthorized remote message writes/test data. |
| T-193 | Email deep-link and SMTP design | completed | Deep | M2/R-007 | auth email/deep-link design, Supabase contract, decision log, memory docs | Supabase changelog/docs review; local auth/config grep; `git diff --check`; context hygiene; commit and push | Closes Q-06 by choosing Resend-backed Supabase custom SMTP, exact HTTPS-first redirect URLs, dev/test custom scheme fallback, required templates/secrets, and iOS callback behavior. Q-07 implementation is blocked until a production auth domain and SMTP credentials are provided/authorized. |
| T-192 | Request wizard persistence decision | completed | Standard | M1/R-006 | customer request store/view wizard presentation, customer request tests, decision log, memory docs | CustomerRequestsStoreTests; XcodeBuildMCP build; `git diff --check`; context hygiene; commit and push | Closes Q-05 by making unpublished request wizard drafts ephemeral to the active sheet: Back/cancel and swipe dismiss discard draft fields/photos and reset to default create state, while publish failures still preserve input and explicit republish remains the only prefilled request flow. Next package is Q-06 email deep-link and SMTP design unless blocked or user chooses another item. |
| T-191 | Groomer private images | completed | Standard | M1/R-005 | groomer profile store/view portfolio presentation, groomer profile tests, private image audit, memory docs | Focused groomer profile tests; XcodeBuildMCP build and launch spot check; `git diff --check`; context hygiene; commit and push | Closes Q-04 by tracking portfolio image-load attempts, distinguishing loading from unavailable portfolio photos, rendering groomer portfolio cards through the shared module image path, and preserving avatar/private-loader contracts. Next package is Q-05 request wizard persistence decision unless blocked or user chooses another item. |
| T-157 | Customer APNs push notification foundation | blocked | Deep | M2 | APNs migrations, dispatcher Edge Function, iOS registration, tests, memory docs | Full T-157 validation recorded in frozen worklogs and task records; context hygiene | Remote DB foundation is applied on `lqmasbuqzvcvtawonjlb`, but dispatcher remains undeployed. Blocked until paid Apple Developer access provides `APNS_KEY_ID`, `APNS_TEAM_ID`, `APNS_TOPIC`, and `APNS_PRIVATE_KEY`. |
