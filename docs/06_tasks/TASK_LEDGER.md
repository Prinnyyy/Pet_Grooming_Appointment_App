# Task Ledger

Track only current, blocked, and recent tasks here so Codex does not continue automatically. Completed rows T-000 through T-145 are archived under `docs/09_frozen/task_ledgers/`. Detailed task records T-001 through T-088 and workflow task records remain archived under `docs/09_frozen/task_records_2026-06-26/`.

Current branch and task-numbering baseline: use `codex/pet-fit-structure-cleanup`; use `T-162` for the next non-APNs task unless the user explicitly names another branch or task ID.

Active blocked task: T-157 is waiting on paid Apple Developer Program access before APNs secrets (`APNS_KEY_ID`, `APNS_TEAM_ID`, `APNS_TOPIC`, `APNS_PRIVATE_KEY`) and Edge Function deploy.

## Recent and Current Tasks

| ID | Task | Status | Mode | Files/Docs | Checks | Notes |
|---|---|---|---|---|---|---|
| T-161 | App Store privacy baseline | completed | Standard | `PrivacyInfo.xcprivacy`, `docs/04_ios/APP_STORE_PRIVACY.md`, `tests/ios/app-store-privacy.test.mjs`, memory docs | RED/GREEN privacy manifest test; `plutil -lint`; `./scripts/ios-build.sh`; built-app manifest presence check; `git diff --check`; context hygiene | Adds the iOS privacy manifest and Groomly 1.0 App Store privacy checklist. Declares no tracking, app-functionality data categories, `UserDefaults` reason `CA92.1`, and app-container file timestamp reason `C617.1`. Real Privacy Policy URL and Support URL remain TBD and blocking before App Store metadata submission. |
| T-160 | Account deletion flow | completed | Deep | `20260707191034_t160_account_deletion.sql`, `supabase/functions/delete-account/`, Auth repository/store/UI/cache cleanup, Node/Swift tests, memory docs | RED/GREEN T-160 migration/function tests; pre-apply dry-run/lint; authorized `supabase db push --linked`; post-apply migration list and metadata SQL; post-apply security/performance advisors; `supabase functions deploy delete-account`; `supabase functions list`; `git diff --check`; `./scripts/ios-build.sh`; targeted `AuthenticationStoreTests`; full `./scripts/ios-test.sh` attempted and failed known T-153 notification ordering test | Applied to `lqmasbuqzvcvtawonjlb` and deployed `delete-account` with JWT verification enabled. Adds controlled account deletion request/anonymization RPCs, service-role status RPCs, user-auth Edge Function that calls Supabase Auth admin `deleteUser(user_id, true)` soft delete, Account double-confirm delete entry, and local snapshot cleanup. Post-apply direct `supabase db push --linked --dry-run`/`supabase db lint --linked` could not rerun because `SUPABASE_DB_PASSWORD` is not configured. Advisors still report baseline `customer_push_tokens` RLS-enabled/no-policy INFO, Auth leaked-password protection WARN, existing performance INFOs, plus the expected new unused-index INFO for the fresh account deletion index. |
| T-159 | Private RPC lint cleanup | completed | Deep | `20260707183427_t159_private_rpc_lint_cleanup.sql`, migration test, memory docs | RED/GREEN T-159 migration test; all migration tests; pre-apply migration list/dry-run; authorized `supabase db push --linked`; post-apply migration list/dry-run; RPC metadata SQL; `supabase db lint`; advisors; diff-check; context hygiene | Applied to `lqmasbuqzvcvtawonjlb`. Rebuilds `app_private.accept_groomer_offer(uuid)` and `app_private.complete_booking(uuid)` without unread variables reported by `supabase db lint`. Lint now reports no schema errors; advisors still only report Auth leaked-password protection. |
| T-158 | Public RPC wrapper hardening | completed | Deep | `20260707174825_t158_public_rpc_wrapper_hardening.sql`, migration tests, RLS/RPC policy, memory docs | RED/GREEN T-158 migration test; all migration tests; authorized `supabase db push --linked`; post-apply dry-run; RPC metadata SQL; rollback wrapper execution SQL; `supabase db advisors`; `supabase db lint`; dispatcher tests; `git diff --check`; context hygiene; `./scripts/ios-build.sh` | Applied to `lqmasbuqzvcvtawonjlb`. Moves 10 advisor-flagged public authenticated `SECURITY DEFINER` RPCs into `app_private` and recreates same public API names as `SECURITY INVOKER` wrappers. Advisors now only report Auth leaked-password protection; the public RPC security-definer warnings are cleared. |
| T-157 | Customer APNs push notification foundation | blocked | Deep | `20260706230112_t157_customer_push_notifications.sql`, T-157 corrective push-token migrations, `supabase/functions/dispatch-customer-push-notifications/`, customer push notification repository/registration coordinator/AppDelegate, notification kind extension, migration/function/Swift tests, memory docs | RED/GREEN T-157 migration test; RED/GREEN dispatcher test; RED/GREEN `CustomerPushNotificationRegistrationStoreTests`; all migration tests; function dispatcher tests; targeted Swift tests; authorized sequential `supabase db push --linked`; post-apply migration list/dry-run; metadata/grants/RPC/trigger SQL; rollback claim/record SQL; rollback authenticated register/unregister SQL; `supabase db lint`; advisors; `supabase secrets list`; `git diff --check`; context hygiene; `./scripts/ios-build.sh` | Remote DB is applied and validated on `lqmasbuqzvcvtawonjlb`, including corrective migrations for Postgres token regex limits, register conflict inference, and table token constraint. User currently has a free Apple Developer account, so APNs Auth Key / Push Notifications capability is not available yet. `dispatch-customer-push-notifications` remains undeployed until paid Apple Developer Program access provides Key ID, Team ID, bundle-topic confirmation, and `.p8` private key. |
| T-156 | Booking handoff read-state persistence | completed | Deep | `20260706220736_t156_booking_handoff_read_state.sql`, Customer request repository/store/view wiring, migration and Store tests, memory docs | RED/GREEN T-156 migration test; RED/GREEN `CustomerRequestsStoreTests`; all migration tests; authorized `db push`; post-apply migration list/dry-run; metadata/RLS/RPC/grant SQL; rollback RLS/RPC validation; zero-residue SQL; lint/advisors; diff-check; iOS build | Applied to `lqmasbuqzvcvtawonjlb`. Replaces pure `UserDefaults` booking handoff acknowledgements with backend-backed customer read state plus local offline fallback. New RPCs are security invoker and not advisor-flagged; baseline advisors unchanged. |
| T-155 | Backfill matching after groomer activation/availability changes | completed | Deep | `20260706213507_t155_backfill_matching.sql`, migration tests, memory docs | RED/GREEN T-155 test; all migration tests; authorized `db push`; post-apply SQL; rollback backfill SQL; remote matching TestOps 8/8 + cleanup; lint/advisors; diff-check; iOS build; `ios-test` had one unrelated T-153 ordering assertion | Applied to `lqmasbuqzvcvtawonjlb`. Adds private reusable request-match insertion, makes `create_grooming_request` reuse it, and backfills after groomer activation plus availability windows, booking preferences, and time-off changes. Private backfill functions are `service_role` only; trigger functions are not externally executable. Baseline advisors unchanged. |
| T-154 | Request expiry conversion | completed | Deep | `20260706211524_t154_request_expiry_conversion.sql`, migration tests, memory docs | RED/GREEN expiry test; all migration tests; authorized `db push`; post-apply SQL for function privileges, cron, rollback behavior, residue; lint/advisors; diff-check; iOS build/test | Applied to `lqmasbuqzvcvtawonjlb`. Adds private `app_private.expire_grooming_requests(integer)`, expires stale active requests plus linked pending offers and active/offered matches, grants execute only to `service_role`, and schedules pg_cron every 5 minutes. Baseline advisors still show pre-existing authenticated SECURITY DEFINER RPC warnings and Auth leaked-password protection. |
| T-153 | Customer in-app notification center | completed | Deep | `customer_notifications` migration, customer notification model/repository/store/view, Customer Home bell navigation, debug wrapper, Swift/SQL tests, screen/feature/memory docs | RED SQL migration test and RED Swift store compile test; `node --test tests/migrations/customer-notifications.test.mjs`; `supabase migration list --linked`; `supabase db push --linked --dry-run`; authorized `supabase db push --linked`; post-apply migration list/dry-run; rollback SQL metadata/RLS/RPC/event-trigger checks; security/performance advisors; XcodeBuildMCP Customer Home bell/list目检; `./scripts/ios-test.sh`; `./scripts/ios-build.sh`; `git diff --check`; `node scripts/context-hygiene-check.mjs` | Applies `20260706203710_t153_customer_notifications.sql` to project `lqmasbuqzvcvtawonjlb`, enabling Customer in-app notifications with current-customer RLS, read-state RPCs, and triggers for request published/cancelled and booking confirmed/cancelled. Security advisor still reports pre-existing public authenticated `SECURITY DEFINER` RPC warnings and Auth leaked-password protection; T-153 mark-read RPCs are security invoker and were not flagged. |
| T-152 | Groomer Offers tab | completed | Standard | Groomer tab routing, `GroomerOffersView`, `GroomerOffersStore`, groomer offer list models/repository adapter, `SCREEN_INVENTORY`, memory docs | RED compile test for missing offers surface; `./scripts/ios-test.sh`; `./scripts/ios-build.sh`; XcodeBuildMCP iPhone 17 Offers-tab目检; `git diff --check`; `node scripts/context-hygiene-check.mjs` | Restores `.offers` to visible groomer tabs, replaces the unreachable Offers fallback with a real submitted-offer list grouped by status, and preserves offer creation/withdrawal inside Board/request detail. No Supabase schema/RLS/RPC/Storage, migration, dependency, seed, commit, push, or remote write changed. |
| T-151 | Recheck active Markdown baseline and harden AGENTS rules | completed | Quick | `AGENTS.md`, memory docs, task/worklog rolling archives, project-structure log | `git diff --check`; `node scripts/context-hygiene-check.mjs`; active stale-reference searches | Confirms active docs now use the T-151/T-152 baseline, adds non-canonical external-report and link-update guardrails, and rolls older active memory rows to frozen archives. No iOS/Supabase/runtime behavior changed. |
| T-150 | Restore correct task baseline and archive external audit drafts | completed | Quick | root external reports, frozen external-agent archive, ignore/search rules, memory docs | `git diff --check`; `node scripts/context-hygiene-check.mjs` | Confirms `codex/pet-fit-structure-cleanup` is the active baseline after T-149, moves the two non-canonical external reports out of root context, and keeps future root copies ignored. No iOS/Supabase/runtime behavior changed. |
| T-149 | Active design document slimming | completed | Quick | `DESIGN_SYSTEM.md`, `UI_IMPLEMENTATION_NOTES.md`, frozen design-note archives, context hygiene budgets, docs indexes, memory docs | `git diff --check`; `node scripts/context-hygiene-check.mjs`; archive/reference checks | Archives full pre-slim design-system and Groomly UI audit text under `docs/09_frozen/design_notes/`, rewrites active design entrypoints as short current-rule/index files, and adds design-doc word budgets to context hygiene. No iOS/Supabase/runtime behavior changed. |
| T-148 | Pointer/template cleanup and Claude path correction | completed | Quick | removed memory decision pointer, removed lightweight workflow/task templates, frozen template archives, Claude docs, docs indexes, memory docs | `git diff --check`; `node scripts/context-hygiene-check.mjs`; stale-reference search | Archives and removes active compatibility/template Markdown that lost daily workflow value. Keeps Claude docs active because the user still uses Claude, but updates them to current `PRODUCT_BRIEF.md`, `UI_IMPLEMENTATION_NOTES.md`, task, memory, and workflow paths. Detailed removed paths are recorded in `WORKLOG.md` and the reorganization log. No iOS/Supabase/runtime behavior changed. |
| T-147 | Active historical pointer and root brief cleanup | completed | Quick | root Fresh brief archive/removal, Groomly prompt pointer removal, product/design/structure indexes, decision link, memory docs | `git diff --check`; `node scripts/context-hygiene-check.mjs`; active reference search | Archives the original root product/engineering brief under `docs/09_frozen/product_briefs/`, removes the active root file, removes the active Groomly design prompt pointer after its full text was already frozen, and reroutes active docs to `docs/01_product/PRODUCT_BRIEF.md` and `docs/08_design/UI_IMPLEMENTATION_NOTES.md`. No iOS/Supabase/runtime behavior changed. |
| T-146 | AI collaboration context indexing and policy trim | completed | Quick | AGENTS, workflow/context docs, backend policy indexes, feature index, TestOps memory, design prompt pointer, frozen archives, context hygiene script, memory docs | `git diff --check`; `node scripts/context-hygiene-check.mjs`; archive/reference/search-path checks | Implements L0-L4 context access rules, stronger task-stop constraints, compact active policy/index files, frozen pre-trim archives, and one read-only context hygiene command. No iOS source, Supabase schema/RLS/RPC/Storage, migrations, dependencies, simulator, or remote state changed. |
