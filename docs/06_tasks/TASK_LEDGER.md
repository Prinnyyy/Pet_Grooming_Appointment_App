# Task Ledger

Track only current, blocked, and recent tasks here so Codex does not continue automatically. Older completed rows through T-190 are archived under `docs/09_frozen/task_ledgers/`; active blocked T-157 remains below. Notes should keep conclusions and pointers only; validation detail belongs in `docs/00_memory/WORKLOG.md` or frozen worklogs. Detailed task records T-001 through T-088 and workflow task records remain archived under `docs/09_frozen/task_records_2026-06-26/`.

Current branch and task-numbering baseline: use `codex/pet-fit-structure-cleanup`; use `T-215` for the next non-APNs task unless the user explicitly names another branch or task ID.

Active blocked task: T-157 is waiting on paid Apple Developer Program access before APNs secrets (`APNS_KEY_ID`, `APNS_TEAM_ID`, `APNS_TOPIC`, `APNS_PRIVATE_KEY`) and Edge Function deploy.

## Recent and Current Tasks

| ID | Task | Status | Mode | Milestone | Files/Docs | Checks | Notes |
|---|---|---|---|---|---|---|---|
| T-214 | Decode and cache tolerance tests | completed | Standard | M7/R-028/Q-26 | marketplace status fallback models, decode/cache tolerance tests, affected status UI copy, roadmap/memory docs | Focused DecodeTolerance test; full `./scripts/ios-test.sh`; `./scripts/ios-build.sh`; `git diff --check`; context hygiene | Closes Q-26 by adding focused tolerance coverage for unknown status enums, malformed dates, null optional pet snapshot fields, and oversized corrupt image cache data, plus safe unknown status fallbacks across marketplace UI surfaces. |
| T-213 | Time and boundary contract tests | completed | Standard | M7/R-028/Q-25 | time-boundary migration contract tests, roadmap/memory docs | Focused time-boundary test; all migration Node tests; supabase-check; `./scripts/ios-build.sh`; `git diff --check`; context hygiene | Closes Q-25 by adding focused backend contract coverage for groomer-local request-day matching, exact availability windows, time-off/advance/daily-capacity gates, expiry boundaries, and service size-band limits. |
| T-212 | Split groomer profile surfaces | completed | Standard | M7/R-026/Q-24 | groomer profile root/account/profile form/services/portfolio/fit/availability/status SwiftUI files, roadmap/memory docs | Full `./scripts/ios-test.sh`; `./scripts/ios-build.sh`; `git diff --check`; context hygiene; commit and push | Closes Q-24 by splitting the oversized `GroomerProfileManagementView.swift` into focused surface files while preserving behavior and keeping shared helper visibility limited to cross-file/test use. |
| T-211 | Split customer requests view | completed | Standard | M7/R-026/Q-23 | customer request root/dashboard/detail/wizard/status SwiftUI files, stale groomer tab model test expectation, roadmap/memory docs | Full `./scripts/ios-test.sh`; `./scripts/ios-build.sh`; `git diff --check`; context hygiene; commit and push | Closes Q-23 by splitting the oversized `CustomerRequestsView.swift` into focused root, dashboard, detail, wizard, and status/preview files while preserving behavior. Validation also aligned an outdated groomer tab test expectation with the existing Alerts tab model. |
| T-210 | Unread badge propagation | completed | Standard | M6/R-025/Q-22 | chat model/store/repository/view, customer/groomer tab badge rules and shared stores, customer home notification store injection, focused chat/badge tests, roadmap/memory docs | ChatStore/TabBadge RED/GREEN; `./scripts/ios-build.sh`; `git diff --check`; context hygiene; commit and push | Closes Q-22 by loading shared notification/chat badge sources at customer/groomer tab roots, showing tab badges for customer Home notifications, customer/groomer Messages unread conversations, and groomer Alerts, and deriving local chat unread counts from latest message sender/time cleared when a thread is opened. |
| T-209 | Local appointment reminders | completed | Standard | M6/R-025/Q-21 | local appointment reminder scheduler, bookings/customer request stores, bookings UI, focused booking/request tests, roadmap/memory docs | Appointment reminder planner/store RED/GREEN; CustomerRequests accept reminder RED/GREEN; `./scripts/ios-build.sh`; `git diff --check`; context hygiene; commit and push | Closes Q-21 by adding local notification reminder planning/scheduling for future confirmed bookings, page-level refusal copy when local notifications are disabled, reminder sync after booking load and offer acceptance, and reminder cancellation after booking cancel/complete. |
| T-208 | Notification domain tests | completed | Standard | M4/M7/R-028/Q-20 | customer/groomer notification models, focused notification tests, roadmap/memory docs | CustomerNotificationsStoreTests and GroomerNotificationsStoreTests RED/GREEN; `./scripts/ios-build.sh`; `git diff --check`; context hygiene; commit and push | Closes Q-20 by covering duplicate concurrent read/mark-all guards, mark-all failure/cancellation state preservation, repository debug events, and unknown notification kind decode fallback for both roles. |
| T-207 | Meta-review and context hygiene | completed | Quick | G0 | current state, task ledger, worklog, frozen rotated rows | `git status --short`; `git diff --check`; context hygiene; commit and push | Runs the required 10-task cadence review after T-206, rotates excess active ledger/worklog rows, confirms active roadmap/queue/current-state pointers align, and keeps Q-20 as the next roadmap package. |
| T-206 | Offer domain tests | completed | Standard | M4/M7/R-028/Q-19 | groomer offer/request store tests, groomer request store, roadmap/memory docs | GroomerOffersStoreTests and GroomerRequestsStoreTests RED/GREEN; `./scripts/ios-build.sh`; `git diff --check`; context hygiene; commit and push | Closes Q-19 by covering offer empty/cancelled list states, stale request and active-offer conflicts, and rejected accepted-offer withdrawal. Also clears stale success notice before local withdraw rejection. |
| T-205 | Foreground state timeliness | completed | Standard | M6/R-025/Q-18 | foreground refresh gate, customer/groomer request/offer/booking/notification views, focused tests, roadmap/memory docs | ForegroundRefreshGateTests RED/GREEN; `./scripts/ios-build.sh`; `git diff --check`; context hygiene; commit and push | Adds a shared foreground refresh gate with initial load, scene-active refresh, realtime-fallback polling, interval throttling, and in-flight dedupe. Customer home/requests/bookings/notifications and groomer requests/offers/bookings/notifications now use the shared refresh path. |
| T-204 | Groomer notification center UI | completed | Standard | M6/R-024/Q-17 | groomer notification model/repository/store/view, groomer tab routing, debug wrapper, focused tests, roadmap/memory docs | GroomerNotificationsStoreTests RED/GREEN; `./scripts/ios-build.sh`; `git diff --check`; context hygiene; commit and push | Adds the groomer Alerts tab, unread badge source, read/mark-all-read state, notification routing to requests/bookings/messages, Supabase repository access, and debug repository events. T-203 remote migration apply still needs explicit authorization before live linked data exists. |
| T-157 | Customer APNs push notification foundation | blocked | Deep | M2 | APNs migrations, dispatcher Edge Function, iOS registration, tests, memory docs | Full T-157 validation recorded in frozen worklogs and task records; context hygiene | Remote DB foundation is applied on `lqmasbuqzvcvtawonjlb`, but dispatcher remains undeployed. Blocked until paid Apple Developer access provides `APNS_KEY_ID`, `APNS_TEAM_ID`, `APNS_TOPIC`, and `APNS_PRIVATE_KEY`. |
