# Task Ledger

Track only current, blocked, and recent tasks here so Codex does not continue automatically. Older completed rows through T-176 are archived under `docs/09_frozen/task_ledgers/`; active blocked T-157 remains below. Notes should keep conclusions and pointers only; validation detail belongs in `docs/00_memory/WORKLOG.md` or frozen worklogs. Detailed task records T-001 through T-088 and workflow task records remain archived under `docs/09_frozen/task_records_2026-06-26/`.

Current branch and task-numbering baseline: use `codex/pet-fit-structure-cleanup`; use `T-193` for the next non-APNs task unless the user explicitly names another branch or task ID.

Active blocked task: T-157 is waiting on paid Apple Developer Program access before APNs secrets (`APNS_KEY_ID`, `APNS_TEAM_ID`, `APNS_TOPIC`, `APNS_PRIVATE_KEY`) and Edge Function deploy.

## Recent and Current Tasks

| ID | Task | Status | Mode | Milestone | Files/Docs | Checks | Notes |
|---|---|---|---|---|---|---|---|
| T-192 | Request wizard persistence decision | completed | Standard | M1/R-006 | customer request store/view wizard presentation, customer request tests, decision log, memory docs | CustomerRequestsStoreTests; XcodeBuildMCP build; `git diff --check`; context hygiene; commit and push | Closes Q-05 by making unpublished request wizard drafts ephemeral to the active sheet: Back/cancel and swipe dismiss discard draft fields/photos and reset to default create state, while publish failures still preserve input and explicit republish remains the only prefilled request flow. Next package is Q-06 email deep-link and SMTP design unless blocked or user chooses another item. |
| T-191 | Groomer private images | completed | Standard | M1/R-005 | groomer profile store/view portfolio presentation, groomer profile tests, private image audit, memory docs | Focused groomer profile tests; XcodeBuildMCP build and launch spot check; `git diff --check`; context hygiene; commit and push | Closes Q-04 by tracking portfolio image-load attempts, distinguishing loading from unavailable portfolio photos, rendering groomer portfolio cards through the shared module image path, and preserving avatar/private-loader contracts. Next package is Q-05 request wizard persistence decision unless blocked or user chooses another item. |
| T-190 | Customer private images | completed | Standard | M1/R-005 | customer request store/view image presentation, customer request tests, private image audit, memory docs | Focused customer requests/pets store tests; XcodeBuildMCP build and launch spot check; `git diff --check`; context hygiene; commit and push | Closes Q-03 by loading pet photo metadata/data into `CustomerRequestsStore`, rendering request wizard pet avatars through `GroomlyModuleImage`, and marking request-photo metadata with missing image data as unavailable instead of silently looking complete. Next package is Q-04 groomer private images unless blocked or user chooses another item. |
| T-189 | Shared private image renderer | completed | Standard | M1/R-005 | private image loader/cache, Supabase Storage image repositories, tests, private image audit, memory docs | Focused `PrivateImageCacheKeyTests`/`PrivateImageLoaderTests`; iOS build; `git diff --check`; context hygiene; commit and push | Closes Q-02 by adding a shared authenticated private-image loader and file cache, replacing direct repository image downloads, preserving legacy groomer avatar fallback, and clearing private image cache on local account cleanup. Next package is Q-03 customer private images unless blocked or user chooses another item. |
| T-188 | Private image contract audit | completed | Deep | M1/R-005 | private image audit, Storage policy, roadmap queue, memory docs | Supabase read-only bucket/RLS queries; Storage code/UI grep; `git diff --check`; context hygiene; commit and push | Closes Q-01 by verifying private buckets, RLS policy shape, authenticated download usage, local cache coverage, rendered surfaces, and gaps. Next package is Q-02 shared private image renderer unless blocked or user chooses another item. |
| T-187 | Roadmap execution queue | completed | Quick | G0/M1-M5 | `ROADMAP.md`, `ROADMAP_EXECUTION_QUEUE.md`, task directory guide, decision log, memory docs | `git diff --check`; context hygiene; commit and push | Adds a bounded execution queue that turns roadmap candidates into adoptable packages without assigning task IDs in ROADMAP. Next product package is Q-01/R-005 private image contract audit unless the user chooses another item. |
| T-186 | Stale workflow 85% wording cleanup and push-failure rule | completed | Quick | G0 | `AGENTS.md`, workflow rules, decision log, memory docs | `git diff --check`; context hygiene; decision rotation; commit and push | Removes active 85% warning/waterline execution rules, keeps 95% as structural review only, fixes the T-185 meta-review date marker, and requires stopping without auto pull/rebase if automatic push fails or is rejected. |
| T-185 | Root external draft cleanup and meta-review | completed | Quick | G0 | frozen external reports, current state, task ledger, worklog, structure log | `git diff --check`; context hygiene; targeted meta-review checks; commit and push | Preserves ignored root external drafts under `docs/09_frozen/external_agent_reports/`, removes root copies, records the scheduled meta-review, and queues T-186 for stale workflow 85% wording found in active rules. |
| T-184 | Context budget workflow alignment | completed | Quick | G0 | `AGENTS.md`, context workflow rules, decision log, frozen external plan, memory docs | `git diff --check`; context hygiene; rotate apply for decision-window overflow; commit and push | Implements Batch B of the context-budget redesign: active rules now point to `context-rotate`, define FIXED/WINDOW/INDEX discipline, remove 85% cleanup semantics from workflow text, and archive the adopted root plan. |
| T-183 | Context budget redesign tooling | completed | Quick | G0 | hygiene/rotate scripts, docs tests, decision log, frozen decisions, memory docs | docs tests; rotate dry-run/apply; context hygiene with and without `rg`; `git diff --check`; commit and push | Implements Batch A of the root redesign plan: 36k hard total, no 85% warning, 95% structural-review warning, 650-word default active Markdown budget, decision-log window checks, shared constants, and deterministic `context-rotate`. Batch B remains T-184. |
| T-182 | Active Markdown waterline cleanup rule | completed | Quick | G0 | `AGENTS.md`, workflow rules, decision log, memory docs, frozen archives | `git diff --check`; context hygiene with no 85% warning; commit and push | Makes the active Markdown 85% waterline a cleanup trigger, clarifies unrelated cleanup vs hygiene cleanup, and archives older ledger/worklog rows to bring active Markdown below the warning line. |
| T-157 | Customer APNs push notification foundation | blocked | Deep | M2 | APNs migrations, dispatcher Edge Function, iOS registration, tests, memory docs | Full T-157 validation recorded in frozen worklogs and task records; context hygiene | Remote DB foundation is applied on `lqmasbuqzvcvtawonjlb`, but dispatcher remains undeployed. Blocked until paid Apple Developer access provides `APNS_KEY_ID`, `APNS_TEAM_ID`, `APNS_TOPIC`, and `APNS_PRIVATE_KEY`. |
