# Task Ledger

Track only current, blocked, and recent tasks here so Codex does not continue automatically. Older completed rows through T-190 are archived under `docs/09_frozen/task_ledgers/`; active blocked T-157 remains below. Notes should keep conclusions and pointers only; validation detail belongs in `docs/00_memory/WORKLOG.md` or frozen worklogs. Detailed task records T-001 through T-088 and workflow task records remain archived under `docs/09_frozen/task_records_2026-06-26/`.

Current branch and task-numbering baseline: use `codex/pet-fit-structure-cleanup`; use `T-223` for the next non-APNs task unless the user explicitly names another branch or task ID.

Active blocked task: T-157 is waiting on paid Apple Developer Program access before APNs secrets (`APNS_KEY_ID`, `APNS_TEAM_ID`, `APNS_TOPIC`, `APNS_PRIVATE_KEY`) and Edge Function deploy.

## Recent and Current Tasks

| ID | Task | Status | Mode | Milestone | Files/Docs | Checks | Notes |
|---|---|---|---|---|---|---|---|
| T-222 | Ideal-operation readiness rehearsal | completed | Deep | M8/R-029/Q-33 | Release readiness evidence, TestOps index, roadmap/queue/memory docs | Supabase CLI help/version; TestOps dry-runs/unit; supabase-check; linked advisors; migration tests; iOS test/build; diff/hygiene | Closes Q-33 with local/read-only readiness evidence after Q-16...Q-32. Advisors completed with non-blocking Auth/APNs configuration and index-tuning findings. No Supabase write, migration, seed, remote TestOps execute, deploy, release upload, or Auth config change. |
| T-221 | Dual-role E2E walkthrough | completed | Deep | M8/R-029/Q-32 | TestOps dual-role evidence record, TestOps results index, roadmap/memory docs | TestOps doctor dry-run; backend smoke5 dry-run; matching baseline dry-run; iOS TestOps launch smoke; `./scripts/testops-unit.sh`; `git diff --check`; context hygiene | Closes Q-32 by recording local no-write evidence for seeded customer/groomer lifecycle planning, matching projections, UI launch wiring, and remaining lifecycle automation gaps. No remote TestOps execute or Supabase write was performed. |
| T-220 | Backend contract negatives | completed | Deep | M7/R-028/Q-31 | notification RLS negative contract tests, rollback validation SQL, RLS/RPC policy index, roadmap/memory docs | Notification negative RED/GREEN; all migration Node tests; `./scripts/supabase-check.sh`; `git diff --check`; context hygiene | Closes Q-31 by adding notification ownership/direct-write/protected-RPC negative contract coverage and a rollback-only SQL script for customer/groomer notification RLS plus service-role-only push delivery RPC checks. No Supabase remote write or migration was performed. |
| T-219 | List pagination/load audit | completed | Standard | M7/R-027/Q-30 | shared pagination model, list repository protocols/wrappers, Supabase list ranges, Bookings/Chat page state, pagination tests, audit doc, roadmap/memory docs | ListPagination RED/GREEN; full `./scripts/ios-test.sh`; `./scripts/ios-build.sh`; `git diff --check`; context hygiene | Closes Q-30 by adding a shared bounded page contract, applying limit+1 range reads to request/offer/booking/message/notification repositories, forwarding pagination through debug wrappers, exposing next-page state for bookings/chat, and documenting remaining UI pagination controls as deferred. |
| T-218 | Meta-review and context hygiene | completed | Quick | G0 | current state, task ledger, worklog, roadmap, frozen rotated rows | `git status --short`; `git diff --check`; context hygiene | Runs the required 10-task cadence review after T-207, rotates excess active ledger/worklog rows, confirms roadmap/queue/current-state pointers align at Q-30/T-219, and records the new meta-review marker. |
| T-217 | Free-tier auth deep link | completed | Deep | M7/R-027/Q-29 | auth repository/store/app callback, Info.plist URL scheme, auth deep-link docs, roadmap/memory docs | AuthenticationStore RED/GREEN; full `./scripts/ios-test.sh`; `./scripts/supabase-check.sh`; `./scripts/ios-build.sh`; `plutil`/diff check; simulator openurl smoke; context hygiene | Closes Q-29 by passing the custom-scheme redirect URL for sign-up, registering the iOS URL scheme, handling supported Auth callbacks via AuthenticationStore with safe error-fragment handling, and documenting that Supabase Auth allow-list/SMTP/HTTPS production-domain work still requires explicit remote configuration authorization. |
| T-216 | State-machine edge tests | completed | Standard | M7/R-028/Q-28 | appointment reminder planner, reminder focused tests, roadmap/memory docs | AppointmentReminderPlan RED/GREEN; full `./scripts/ios-test.sh`; `./scripts/ios-build.sh`; `git diff --check`; context hygiene | Closes Q-28 by adding focused state-machine coverage for duplicate booking reminder rows and making local appointment reminder planning idempotent by stable reminder identifier while preserving existing draft discard, republish mapping, and foreground refresh dedupe coverage. |
| T-215 | Republish hardening | completed | Standard | M7/R-027/Q-27 | customer request store, republish focused tests, roadmap/memory docs | CustomerRequestsStore RED/GREEN; full `./scripts/ios-test.sh`; `./scripts/ios-build.sh`; `git diff --check`; context hygiene | Closes Q-27 by keeping cancelled/expired original request details usable when request-photo metadata is unavailable, skipping missing/oversized photo copies, defaulting expired preferred windows to a future range, and preventing cancelled-booking republish when the original request is unavailable. |
| T-214 | Decode and cache tolerance tests | completed | Standard | M7/R-028/Q-26 | marketplace status fallback models, decode/cache tolerance tests, affected status UI copy, roadmap/memory docs | Focused DecodeTolerance test; full `./scripts/ios-test.sh`; `./scripts/ios-build.sh`; `git diff --check`; context hygiene | Closes Q-26 by adding focused tolerance coverage for unknown status enums, malformed dates, null optional pet snapshot fields, and oversized corrupt image cache data, plus safe unknown status fallbacks across marketplace UI surfaces. |
| T-213 | Time and boundary contract tests | completed | Standard | M7/R-028/Q-25 | time-boundary migration contract tests, roadmap/memory docs | Focused time-boundary test; all migration Node tests; supabase-check; `./scripts/ios-build.sh`; `git diff --check`; context hygiene | Closes Q-25 by adding focused backend contract coverage for groomer-local request-day matching, exact availability windows, time-off/advance/daily-capacity gates, expiry boundaries, and service size-band limits. |
| T-212 | Split groomer profile surfaces | completed | Standard | M7/R-026/Q-24 | groomer profile root/account/profile form/services/portfolio/fit/availability/status SwiftUI files, roadmap/memory docs | Full `./scripts/ios-test.sh`; `./scripts/ios-build.sh`; `git diff --check`; context hygiene; commit and push | Closes Q-24 by splitting the oversized `GroomerProfileManagementView.swift` into focused surface files while preserving behavior and keeping shared helper visibility limited to cross-file/test use. |
| T-157 | Customer APNs push notification foundation | blocked | Deep | M2 | APNs migrations, dispatcher Edge Function, iOS registration, tests, memory docs | Full T-157 validation recorded in frozen worklogs and task records; context hygiene | Remote DB foundation is applied on `lqmasbuqzvcvtawonjlb`, but dispatcher remains undeployed. Blocked until paid Apple Developer access provides `APNS_KEY_ID`, `APNS_TEAM_ID`, `APNS_TOPIC`, and `APNS_PRIVATE_KEY`. |
