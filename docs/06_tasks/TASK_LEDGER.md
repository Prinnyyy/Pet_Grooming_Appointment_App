# Task Ledger

Track only current, blocked, and recent tasks here so Codex does not continue automatically. Detailed completed rows T-000 through T-127 are archived at `docs/09_frozen/task_ledgers/TASK_LEDGER_T-000_TO_T-127_2026-07-01.md`. Detailed standalone task records T-001 through T-088 and workflow task records remain archived under `docs/09_frozen/task_records_2026-06-26/`.

Current branch and task-numbering baseline: use `codex/pet-fit-structure-cleanup` and assign new bugfix/iteration work to `T-146` unless the user explicitly names another branch or task ID.

Active blocked task: none.

## Recent and Current Tasks

| ID | Task | Status | Mode | Files/Docs | Checks | Notes |
|---|---|---|---|---|---|---|
| T-128 | Supabase migration history repair and CLI workflow docs | completed | Deep | Remote `supabase_migrations.schema_migrations`, CustomerPetsView, MIGRATION_RULES.md, TOOLING_POLICY.md, SUPABASE_CONTRACT.md, DECISION_LOG.md, CURRENT_STATE.md, WORKLOG.md, TASK_LEDGER.md | Root-cause `migration list --linked`; failing `db push --linked --dry-run` reproduction; remote history repair reverting 4 remote-only versions and applying 4 local canonical versions; post-repair `migration list --linked`; post-repair `db push --linked --dry-run` returning `Remote database is up to date.`; `./scripts/supabase-check.sh`; `./scripts/ios-build.sh`; `git diff --check` | Fixes the historical remote/local migration-version drift that blocked linked dry-runs. Local migration filenames are now the canonical remote history for T-044/T-060/T-071/T-072, and future Supabase workflow requires `migration list`, `db push --dry-run`, approved `db push`, and final dry-run verification. Also removes the CustomerPetsView PhotosPicker Sendable/main-actor warning observed during simulator launch. No business schema, RLS, RPC, or Storage object was changed. |
| T-129 | Local bulk test account resource profiles | completed | Quick | `docs/02_architecture/test_resources/T-129_GROOMER_TEST_PROFILES.md`, `docs/02_architecture/test_resources/T-129_CUSTOMER_TEST_PROFILES.md`, fixture policy docs, memory docs | `git diff --check` | Adds reviewable local seed resources for 50 groomer accounts and 50 customer accounts, with each customer owning one dog and one cat. No Supabase remote write, Auth user creation, seed script, schema, RLS, RPC, Storage, Swift, build, or runtime behavior change. |
| T-130 | LA/OC public-address refinement for bulk test resources | completed | Quick | `docs/02_architecture/test_resources/T-129_GROOMER_TEST_PROFILES.md`, `docs/02_architecture/test_resources/T-129_CUSTOMER_TEST_PROFILES.md`, memory docs | parsed resource validation; `git diff --check` | Replaces the earlier broad California seed addresses with real public institution addresses restricted to Los Angeles County and Orange County, records official source/safety notes, adds groomer bios, aligns groomer business names to the address regions, normalizes groomer years to the app's 0-5 range, and keeps all resources documentation-only with no remote Supabase writes. |
| T-131 | Request profile-address autofill and shared address search | completed | Standard | GroomlyAddressSearch, CustomerRequestsStore/View, CustomerPetsView, CustomerTabView, AuthenticatedEntryView, GroomerProfileManagementView, CustomerRequestFeatureTests, memory docs | RED focused tests; GREEN focused tests; ios-build; simulator launch; diff-check | Adds a `Use Profile Address` button to the customer grooming request address module. The button reads the saved customer profile address through the existing customer profile repository, fills street/city/state/ZIP when complete, and shows a unified global bottom error prompt when no saved address exists or the profile address cannot load. Customer request and groomer profile address autocomplete now share one MapKit-backed address search implementation, removing duplicated local search classes and keeping groomer address selection on the same real-address autofill path as customer requests. No Supabase schema/RLS/RPC/Storage change or remote write. |
| T-132 | Customer profile address autocomplete and sheet feedback visibility | completed | Standard | GroomlyAddressSearch, GroomlyFeedbackPrimitives, CustomerProfileSettingsView, CustomerRequestsView, CustomerProfileFeatureTests, AppEntryModelsTests, memory docs | RED focused tests; GREEN focused tests; ios-build; simulator launch; diff-check | Customer Profile address fields now use the shared MapKit-backed address suggestion/resolve path instead of plain text-only entry. The request wizard sheet mounts the same global feedback overlay with sheet-specific bottom clearance, so `Use Profile Address` can show the no-saved-address prompt while the sheet is open. No second toast system, Supabase schema/RLS/RPC/Storage change, or remote write. |
| T-133 | Customer profile save permission fix | completed | Standard | SupabaseCustomerProfileRepository, CustomerProfileFeatureTests, memory docs | Debug Console event trace; RED focused test; GREEN focused test; ios-build; diff-check | Fixes Customer Profile save failures caused by `customer_profiles` upsert encoding `user_id` on existing-row updates while the backend intentionally grants updates only to safe address/contact columns. Existing customer profile rows now use `update` with a payload that omits `user_id`; only missing rows fall back to `insert` with `user_id`. No Supabase schema/RLS/RPC/Storage change, migration, or remote write. |
| T-134 | Remote seed T-129 groomer test accounts | completed | Standard | `scripts/seed-t129-groomers.mjs`, T-129 groomer test resource docs, memory docs, Supabase remote Auth/data | script dry-run; authorized remote seed execute; idempotency re-run; seeded account password-login smoke; linked SQL count verification; diff-check | Creates the 50 documented T-129 groomer test accounts on remote project `lqmasbuqzvcvtawonjlb` with confirmed Auth email/password login, groomer role metadata, `profiles`, active `groomer_profiles`, 150 services, 246 availability windows, 50 booking preferences, and 508 fit claims. The reusable seed script is idempotent for `groomly.groomerNNN@example.com`, replaces profile-owned seed rows, and refuses to overwrite non-groomer public profiles. No Supabase schema/RLS/RPC/Storage change or migration. |
| T-135 | Remote seed T-129 customer test accounts | completed | Standard | `scripts/seed-t129-customers.mjs`, T-129 customer test resource docs, memory docs, Supabase remote Auth/data | script dry-run; authorized remote seed execute; idempotency re-run; seeded account password-login smoke; authenticated RLS pet read smoke; supabase-check; diff-check | Creates the 50 documented T-129 customer test accounts on remote project `lqmasbuqzvcvtawonjlb` with confirmed Auth email/password login, customer role metadata, `profiles`, `customer_profiles` contact/address fields, and 100 active owned pet profiles. The reusable seed script is idempotent for `groomly.customerNNN@example.com`, replaces owned pet rows for seed customers, validates app pet taxonomy/weight constraints, and refuses to overwrite non-customer public profiles. No Supabase schema/RLS/RPC/Storage change or migration. |
| T-136 | Unified TestOps automation module | completed | Standard | `docs/04_ios/testops/`, `scripts/testops.mjs`, `scripts/ios-testops-e2e.sh`, TestOps launch args, Debug Console TestOps section, debug/config/auth tests, UI launch smoke, memory docs | RED/GREEN focused TestOps tests; `node --check scripts/testops.mjs`; `node scripts/testops.mjs doctor --dry-run`; `node scripts/testops.mjs run backend --scenario marketplace_full_lifecycle`; `./scripts/ios-testops-e2e.sh marketplace_full_lifecycle`; booted simulator JSONL category-test verification; `./scripts/supabase-check.sh`; `./scripts/ios-test.sh`; `./scripts/ios-build.sh`; `git diff --check` | Adds the unified TestOps module for backend lifecycle automation, tagged cleanup/report scaffolding, run/scenario launch metadata, Debug Console `category=test` context, and XCUITest launch wiring. Remote backend execution remains blocked unless an operator explicitly uses `--execute` with `TESTOPS_REMOTE_WRITE_APPROVED=1`; no Supabase schema/RLS/RPC/Storage change or remote write was performed. |
| T-137 | TestOps unit tests and smoke case catalog | completed | Standard | `scripts/testops.mjs`, `scripts/testops-core.mjs`, `scripts/testops-unit.sh`, `tests/testops/`, `docs/04_ios/testops/`, iOS testing docs, memory docs | RED `node --test tests/testops/testops-core.test.mjs`; GREEN `./scripts/testops-unit.sh`; `node --check scripts/testops.mjs`; `node --check scripts/testops-core.mjs`; `node scripts/testops.mjs doctor --dry-run`; `node scripts/testops.mjs run backend --scenario marketplace_full_lifecycle --matrix smoke5`; `node scripts/testops.mjs cleanup --run-id TESTOPS-DRYRUN`; `./scripts/supabase-check.sh`; `./scripts/ios-test.sh`; `./scripts/ios-build.sh`; authorized remote `smoke5` lifecycle 5/5 passed after fixing the plan to send a valid 15-mile travel radius; SQL final-state verification passed; tagged cleanup left 0 requests/offers/reviews; `git diff --check` | Splits TestOps into a thin CLI plus tested core module, adds Node built-in tests for seed parsing, option parsing, safety gates, plan/matrix generation, redaction/report sanitization, and cleanup planning, and documents the first five backend smoke cases in `smoke5`. Remote smoke initially exposed an invalid `customer_comes_to_groomer` travel-radius plan value; the core now matches app behavior with `travelRadiusMiles=15`. No Supabase schema/RLS/RPC/Storage change was performed, and remote lifecycle data was cleaned by `TESTOPS:<run_id>` tag. |
| T-138 | Supabase tooling and credential usage rules update | completed | Quick | `TOOLING_POLICY.md`, `MIGRATION_RULES.md`, `SUPABASE_CONTRACT.md`, `DECISION_LOG.md`, memory docs | `supabase --version`; sequential `supabase migration list --linked`; Supabase CLI docs/changelog reference check; `git diff --check` | Updates local Supabase operating rules so linked CLI commands are single-flight only, `cli_login_postgres`/SASL failures are triaged as possible transient login-role contention before any repair, ignored Supabase credential files may be inspected only under explicit current-operation authorization into ephemeral env vars, and MCP SQL remains a read-only verification/tagged-cleanup fallback rather than a migration path. Later T-139 clarifies the local `supabase_api_key` value is `sb_secret_...`, not a JWT service-role key. No Supabase schema/RLS/RPC/Storage or remote data changed. |
| T-139 | Supabase CLI credential taxonomy docs | completed | Quick | `.gitignore`, `TOOLING_POLICY.md`, `MIGRATION_RULES.md`, `SUPABASE_CONTRACT.md`, TestOps docs, T-129 seed resource docs, decision/project-structure/memory docs | Prior sequential `supabase projects list`, `supabase migration list --linked`, and `supabase db push --linked --dry-run`; Supabase CLI/API-key docs reference check; `git diff --check` | Documents that local CLI is already logged in/linked and does not currently need `SUPABASE_DB_PASSWORD`; separates `sbp_...` CLI PAT, remote DB password, publishable key, `sb_secret_...` project secret, and JWT-shaped service-role key. Its TestOps limitation was superseded by T-145; seed scripts still cannot use `sb_secret_...` as a legacy JWT service-role key. No schema/RLS/RPC/Storage/remote data changed. |
| T-140 | Context footprint and memory archive trim | completed | Quick | memory/workflow docs and frozen context archives | diff-check; word-count check; archive-reference check; active-path check | Trims active WORKLOG, TASK_LEDGER, and CURRENT_STATE into fast-path files; preserves full pre-trim history under `docs/09_frozen/`; no iOS/Supabase/runtime behavior change |
| T-141 | Automatic context hygiene workflow rules | completed | Quick | AGENTS, workflow docs, active/frozen indexes, memory docs | diff-check; rule search; path existence check; active-path check; Markdown link check; word-count check | Adds post-task context hygiene thresholds and rolling archive rules so active memory/task files stay compact after future tasks; no iOS/Supabase/runtime behavior change |
| T-142 | Markdown information architecture and search hygiene optimization | completed | Quick | `.rgignore`, decision logs, backend contract docs, test-resource docs, workflow/index/memory docs, frozen backend contract archive | diff-check; word-count checks; Markdown link check; reference checks; seed parser dry-runs | Makes `docs/07_decisions/DECISION_LOG.md` the single complete decision log and turns the memory copy into a pointer; archives the long Supabase contract and replaces the active file with a fast-path backend contract index; adds `.rgignore` so broad searches skip frozen archives, generated artifacts, T-129 seed tables, and Groomly HTML by default; adds a test-resource index and marks T-129 profile docs as machine-readable parser inputs. No iOS/Supabase/runtime behavior or remote state changed. |
| T-143 | TestOps edge unit tests and safety hardening | completed | Standard | `scripts/testops-core.mjs`, `scripts/testops-unit.sh`, `tests/testops/testops-edge.test.mjs`, TestOps docs, memory docs | RED/GREEN `node --test tests/testops/testops-edge.test.mjs`; `node --test tests/testops/testops-core.test.mjs`; `./scripts/testops-unit.sh`; `node --check scripts/testops-core.mjs`; `node --check scripts/testops.mjs`; `git diff --check` | Adds edge coverage for malformed/duplicate seed resources, unsafe run ids, service-role key type mistakes, stronger redaction, zero-tag cleanup, and zero-match lifecycle failures. At T-143, TestOps rejected modern `sb_secret_...` service credentials; T-145 later replaced that with `apikey`-only support. No Supabase schema/RLS/RPC/Storage, seed data, iOS, dependency, or remote state changed. |
| T-144 | Matching TestOps | completed | Standard | `scripts/testops.mjs`, `scripts/testops-core.mjs`, `tests/testops/testops-matching.test.mjs`, TestOps docs, memory docs | RED/GREEN `node --test tests/testops/testops-matching.test.mjs`; `./scripts/testops-unit.sh`; matching dry-run; `node --check scripts/testops-core.mjs`; `node --check scripts/testops.mjs`; `git diff --check` | Adds `request_matching_eval` with `matching_baseline`, covering target groomer include/exclude assertions, same-day-capacity vs exact preferred-window reason fragments, and local hard-filter projection for service type, location mode, and request-day availability from T-129 seed metadata. Remote matching execute remained gated during T-144 and was run later in T-145. No Supabase schema/RLS/RPC/Storage, seed data, iOS, dependency, or remote state changed. |
| T-145 | TestOps modern Supabase secret support and authorized remote matching run | completed | Standard | `scripts/testops-core.mjs`, `scripts/testops.mjs`, `tests/testops/testops-edge.test.mjs`, Supabase/TestOps docs, decision log, results index, memory docs | RED/GREEN edge tests for `sb_secret` service credentials and Auth token normalization; `./scripts/testops-unit.sh`; authorized remote `matching_baseline` 8/8 with cleanup; read-only zero-residue verification; `node --check` scripts; `git diff --check` | Updates TestOps so remote execute/cleanup accepts either legacy service-role JWTs or modern `SUPABASE_SECRET_KEY=sb_secret_...` as `apikey`-only server credentials. Authorized remote matching run `TESTOPS-MATCH-REMOTE-20260702-*` passed all 8 cases and cleanup left zero tagged requests. No Supabase schema/RLS/RPC/Storage, seed data, iOS, dependency, or persistent remote test data changed. Seed scripts still require JWT-shaped legacy service-role keys. |
