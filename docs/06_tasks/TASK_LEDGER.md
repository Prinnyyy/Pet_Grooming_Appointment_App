# Task Ledger

Track only current, blocked, and recent tasks here so Codex does not continue automatically. Older completed rows through T-160 are archived under `docs/09_frozen/task_ledgers/`; active blocked T-157 remains below. Detailed task records T-001 through T-088 and workflow task records remain archived under `docs/09_frozen/task_records_2026-06-26/`.

Current branch and task-numbering baseline: use `codex/pet-fit-structure-cleanup`; use `T-175` for the next non-APNs task unless the user explicitly names another branch or task ID.

Active blocked task: T-157 is waiting on paid Apple Developer Program access before APNs secrets (`APNS_KEY_ID`, `APNS_TEAM_ID`, `APNS_TOPIC`, `APNS_PRIVATE_KEY`) and Edge Function deploy.

## Recent and Current Tasks

| ID | Task | Status | Mode | Milestone | Files/Docs | Checks | Notes |
|---|---|---|---|---|---|---|---|
| T-174 | Decision log prearchive and governance review intake | completed | Quick | G0 | decision log snapshot/index, frozen external review plan, frozen indexes, memory docs, ledger/worklog archives | `git diff --check`; context hygiene; decision snapshot and git-history checks | Implements batch A from the governance review fix plan: records one-time historical commit exception for `01c80e4` and `6d1da33`, snapshots and trims the active decision log, and archives the adopted root review plan. No history rewrite, commit, push, iOS, Supabase, runtime, or simulator change. |
| T-173 | Main governance divergence reconciliation | completed | Quick | G0 | `GITHUB_RULES.md`, decision log, roadmap, memory docs, ledger/worklog archives | read-only git review of `2fddf7b`; `git diff --check`; context hygiene; targeted divergence search | Reviews `main`/`origin/main` commit `2fddf7b` and records it as superseded by this branch's governance architecture. Do not merge it back; future `main` alignment should carry this branch forward or cherry-pick only explicitly reviewed non-stale changes. |
| T-172 | Rule-change process | completed | Quick | G0 | `AGENTS.md`, `CLAUDE.md`, workflow rules, decision log, roadmap, memory docs, ledger/worklog archives | `git diff --check`; context hygiene; targeted rule search | Requires changes to `AGENTS.md`, `CLAUDE.md`, or `docs/05_workflow/**` to be standalone numbered tasks with a decision-log entry and context hygiene. Adds a stop condition for incidental rule changes. No iOS/Supabase/runtime behavior changed. |
| T-171 | Periodic meta-review template | completed | Quick | G0 | `META_REVIEW_TEMPLATE.md`, task/docs indexes, roadmap, memory docs, ledger/worklog archives | `git diff --check`; context hygiene | Adds a compact template requiring a documentation-governance review every 10 completed tasks or weekly, with status, diff, context hygiene, and targeted search checks. No iOS/Supabase/runtime behavior changed. |
| T-170 | Context hygiene v3 fact checks | completed | Quick | G0 | `context-hygiene-check.mjs`, hygiene tests, Feature Index, Supabase contract, migration rules, context workflow, roadmap, memory docs, ledger/worklog archives | RED/GREEN hygiene tests; context hygiene; `git diff --check` | Adds last-verified freshness checks, migration mirror count validation, ROADMAP-to-ledger evidence/status checks, and Feature Index read-first path coverage. Records 55 local migration mirrors and last-verified markers for key indexes. No iOS/Supabase/runtime behavior changed. |
| T-169 | Active document structure reduction | completed | Quick | G0 | `REORGANIZATION_LOG.md`, `CLAUDE.md`, docs/task/frozen indexes, roadmap, memory docs, ledger/worklog archives | `git diff --check`; context hygiene; targeted stale-reference searches | Shrinks the active structure log into an index, moves old `CLAUDE_reference/` snapshots and generic handoff/review templates to frozen, updates indexes, and rolls T-154/T-159 out of active ledger/worklog windows. No iOS/Supabase/runtime behavior changed. |
| T-168 | Testing and migration workflow rules | completed | Quick | G0 | `preflight.sh`, preflight tests, iOS testing docs, migration rules, roadmap, memory docs, ledger/worklog archives | RED/GREEN preflight test; `./scripts/preflight.sh`; `git diff --check`; context hygiene | Preflight now runs local migration and Edge Function Node tests and requires roadmap/Git docs. Runbooks require local tests before authorized remote validation and record Edge Function/pg_cron gates. |
| T-167 | Managed ROADMAP adoption | completed | Quick | G0 | `ROADMAP.md`, README indexes, workflow/context rules, task ledger, decision log, memory docs, hygiene script/tests | RED/GREEN ROADMAP budget test; `git diff --check`; context hygiene | Creates the governed roadmap, converts external V1.0 drafts into milestone/candidate indexes without reusing old task IDs, adds ledger milestone mapping, and keeps task numbering in `TASK_LEDGER.md`. No iOS/Supabase/runtime behavior changed. |
| T-166 | Git/GitHub rules hardening | completed | Quick | G0 | `GITHUB_RULES.md`, README indexes, `TOOLING_POLICY.md`, decision log, memory docs, ledger archive | `git diff --check`; context hygiene | Rewrites Git rules around `T-xxx: <type>: <summary>` commits, same-task commit scope, checkpoint commits, push/PR/tag approval gates, branch cleanup, and explicit `main` reconciliation. No iOS/Supabase/runtime behavior changed. |
| T-165 | Context hygiene v2 truth checks | completed | Standard | G0 | `scripts/context-hygiene-check.mjs`, `tests/docs/context-hygiene-check.test.mjs`, `CONTEXT_AND_RECOVERY.md`, worklog archive, memory docs | RED/GREEN context hygiene tests; context hygiene v2; `git diff --check` | Adds fixture-root support, branch/latest/next task consistency checks, active Markdown total budget, expanded file budgets, worklog/ledger rolling-window checks, and clean missing-`rg` errors. Moves older T-155/T-157 worklog entries to frozen so v2 passes. No iOS/Supabase/runtime behavior changed. |
| T-164 | Backend contract catch-up | completed | Quick | G0 | `SUPABASE_CONTRACT.md`, `RLS_RPC_POLICY.md`, memory docs | `git diff --check`; context hygiene; migration-object reference search | Updates active backend contract indexes for T-153 through T-160 objects: customer notifications, handoff acknowledgements, push tokens, account deletion requests, request-expiry/backfill automation, Edge Function state, and new controlled/service-role RPCs. No Supabase remote command or runtime behavior changed. |
| T-163 | Docs governance truth cleanup | completed | Quick | G0 | README/Claude/product/design/workflow docs, feature index, decision log, frozen external report, current-state snapshot, ledger archive, memory docs | `git diff --check`; context hygiene; stale-notification wording search | Archives the root governance plan as non-authoritative review input, records notification/APNs decisions, replaces stale active-task and push-deferred wording, fixes Offers routing, and keeps active task/current-state docs under budget. No iOS/Supabase/runtime behavior changed. |
| T-162 | Cancelled request/booking repost flow | completed | Standard | M2 | Customer request Store/View, BookingsView, CustomerTabView, Customer request feature tests, memory docs | RED/GREEN targeted `CustomerRequestsStoreTests`; `git diff --check`; `./scripts/ios-build.sh`; context hygiene | Adds Customer “Create New Request” entry points from cancelled requests and cancelled bookings. The flow pre-fills the existing five-step wizard at Review from the original request, copies cached request photos into the new draft upload path, and publishes through `createRequest` without reusing the original request id. No Supabase schema/RLS/RPC migration. |
| T-161 | App Store privacy baseline | completed | Standard | M3 | `PrivacyInfo.xcprivacy`, `docs/04_ios/APP_STORE_PRIVACY.md`, `tests/ios/app-store-privacy.test.mjs`, memory docs | RED/GREEN privacy manifest test; `plutil -lint`; `./scripts/ios-build.sh`; built-app manifest presence check; `git diff --check`; context hygiene | Adds the iOS privacy manifest and Groomly 1.0 App Store privacy checklist. Declares no tracking, app-functionality data categories, `UserDefaults` reason `CA92.1`, and app-container file timestamp reason `C617.1`. Real Privacy Policy URL and Support URL remain TBD and blocking before App Store metadata submission. |
| T-157 | Customer APNs push notification foundation | blocked | Deep | M2 | `20260706230112_t157_customer_push_notifications.sql`, T-157 corrective push-token migrations, `supabase/functions/dispatch-customer-push-notifications/`, customer push notification repository/registration coordinator/AppDelegate, notification kind extension, migration/function/Swift tests, memory docs | RED/GREEN T-157 migration test; RED/GREEN dispatcher test; RED/GREEN `CustomerPushNotificationRegistrationStoreTests`; all migration tests; function dispatcher tests; targeted Swift tests; authorized sequential `supabase db push --linked`; post-apply migration list/dry-run; metadata/grants/RPC/trigger SQL; rollback claim/record SQL; rollback authenticated register/unregister SQL; `supabase db lint`; advisors; `supabase secrets list`; `git diff --check`; context hygiene; `./scripts/ios-build.sh` | Remote DB is applied and validated on `lqmasbuqzvcvtawonjlb`, including corrective migrations for Postgres token regex limits, register conflict inference, and table token constraint. User currently has a free Apple Developer account, so APNs Auth Key / Push Notifications capability is not available yet. `dispatch-customer-push-notifications` remains undeployed until paid Apple Developer Program access provides Key ID, Team ID, bundle-topic confirmation, and `.p8` private key. |
