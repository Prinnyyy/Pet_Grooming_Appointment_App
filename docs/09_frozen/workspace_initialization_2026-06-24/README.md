# Workspace Initialization Archive

This archive preserves the original Codex workspace initialization prompt.

- `CODEX_WORKSPACE_INIT.md`: historical setup prompt used to scaffold the early documentation/workflow structure.

The active repository entrypoints are now `AGENTS.md`, `README.md`, `docs/README.md`, and `docs/10_project_structure/README.md`.
