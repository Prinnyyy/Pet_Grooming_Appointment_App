# Archived Task Ledger Row: T-154

Moved out of `docs/06_tasks/TASK_LEDGER.md` during T-169 to keep the active ledger within the 15-row recovery window.

| ID | Task | Status | Mode | Milestone | Files/Docs | Checks | Notes |
|---|---|---|---|---|---|---|---|
| T-154 | Request expiry conversion | completed | Deep | M1/M2 | `20260706211524_t154_request_expiry_conversion.sql`, migration tests, memory docs | RED/GREEN expiry test; all migration tests; authorized `db push`; post-apply SQL for function privileges, cron, rollback behavior, residue; lint/advisors; diff-check; iOS build/test | Applied to `lqmasbuqzvcvtawonjlb`. Adds private `app_private.expire_grooming_requests(integer)`, expires stale active requests plus linked pending offers and active/offered matches, grants execute only to `service_role`, and schedules pg_cron every 5 minutes. Baseline advisors still show pre-existing authenticated SECURITY DEFINER RPC warnings and Auth leaked-password protection. |
