# Archived Task Ledger Row: T-155

Moved out of `docs/06_tasks/TASK_LEDGER.md` during T-170 to keep the active ledger within the 15-row recovery window.

| ID | Task | Status | Mode | Milestone | Files/Docs | Checks | Notes |
|---|---|---|---|---|---|---|---|
| T-155 | Backfill matching after groomer activation/availability changes | completed | Deep | M2 | `20260706213507_t155_backfill_matching.sql`, migration tests, memory docs | RED/GREEN T-155 test; all migration tests; authorized `db push`; post-apply SQL; rollback backfill SQL; remote matching TestOps 8/8 + cleanup; lint/advisors; diff-check; iOS build; `ios-test` had one unrelated T-153 ordering assertion | Applied to `lqmasbuqzvcvtawonjlb`. Adds private reusable request-match insertion, makes `create_grooming_request` reuse it, and backfills after groomer activation plus availability windows, booking preferences, and time-off changes. Private backfill functions are `service_role` only; trigger functions are not externally executable. Baseline advisors unchanged. |
