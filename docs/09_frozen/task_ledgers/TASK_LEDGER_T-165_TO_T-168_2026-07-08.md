# Archived Task Ledger Rows - T-165 to T-168

Source: `docs/06_tasks/TASK_LEDGER.md` before T-179 lowered the active ledger window to 12 rows.

| ID | Task | Status | Mode | Milestone | Files/Docs | Checks | Notes |
|---|---|---|---|---|---|---|---|
| T-168 | Testing and migration workflow rules | completed | Quick | G0 | `preflight.sh`, preflight tests, iOS testing docs, migration rules, roadmap, memory docs, ledger/worklog archives | RED/GREEN preflight test; `./scripts/preflight.sh`; `git diff --check`; context hygiene | Preflight now runs local migration and Edge Function Node tests and requires roadmap/Git docs. Runbooks require local tests before authorized remote validation and record Edge Function/pg_cron gates. |
| T-167 | Managed ROADMAP adoption | completed | Quick | G0 | `ROADMAP.md`, README indexes, workflow/context rules, task ledger, decision log, memory docs, hygiene script/tests | RED/GREEN ROADMAP budget test; `git diff --check`; context hygiene | Creates the governed roadmap, converts external V1.0 drafts into milestone/candidate indexes without reusing old task IDs, adds ledger milestone mapping, and keeps task numbering in `TASK_LEDGER.md`. No iOS/Supabase/runtime behavior changed. |
| T-166 | Git/GitHub rules hardening | completed | Quick | G0 | `GITHUB_RULES.md`, README indexes, `TOOLING_POLICY.md`, decision log, memory docs, ledger archive | `git diff --check`; context hygiene | Rewrites Git rules around `T-xxx: <type>: <summary>` commits, same-task commit scope, checkpoint commits, push/PR/tag approval gates, branch cleanup, and explicit `main` reconciliation. No iOS/Supabase/runtime behavior changed. |
| T-165 | Context hygiene v2 truth checks | completed | Standard | G0 | `scripts/context-hygiene-check.mjs`, `tests/docs/context-hygiene-check.test.mjs`, `CONTEXT_AND_RECOVERY.md`, worklog archive, memory docs | RED/GREEN context hygiene tests; context hygiene v2; `git diff --check` | Adds fixture-root support, branch/latest/next task consistency checks, active Markdown total budget, expanded file budgets, worklog/ledger rolling-window checks, and clean missing-`rg` errors. Moves older T-155/T-157 worklog entries to frozen so v2 passes. No iOS/Supabase/runtime behavior changed. |
