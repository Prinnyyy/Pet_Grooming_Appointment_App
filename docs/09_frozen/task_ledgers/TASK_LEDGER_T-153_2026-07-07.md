# Archived Task Ledger Row: T-153

Source: `docs/06_tasks/TASK_LEDGER.md`

Archived on 2026-07-07 during T-168 to keep the active task ledger within the context hygiene 15-row window.

Row is preserved verbatim.

| ID | Task | Status | Mode | Milestone | Files/Docs | Checks | Notes |
|---|---|---|---|---|---|---|---|
| T-153 | Customer in-app notification center | completed | Deep | M2 | `customer_notifications` migration, customer notification model/repository/store/view, Customer Home bell navigation, debug wrapper, Swift/SQL tests, screen/feature/memory docs | RED SQL migration test and RED Swift store compile test; `node --test tests/migrations/customer-notifications.test.mjs`; `supabase migration list --linked`; `supabase db push --linked --dry-run`; authorized `supabase db push --linked`; post-apply migration list/dry-run; rollback SQL metadata/RLS/RPC/event-trigger checks; security/performance advisors; XcodeBuildMCP Customer Home bell/list目检; `./scripts/ios-test.sh`; `./scripts/ios-build.sh`; `git diff --check`; `node scripts/context-hygiene-check.mjs` | Applies `20260706203710_t153_customer_notifications.sql` to project `lqmasbuqzvcvtawonjlb`, enabling Customer in-app notifications with current-customer RLS, read-state RPCs, and triggers for request published/cancelled and booking confirmed/cancelled. Security advisor still reports pre-existing public authenticated `SECURITY DEFINER` RPC warnings and Auth leaked-password protection; T-153 mark-read RPCs are security invoker and were not flagged. |
