# Archived Task Ledger Row - T-162

Source: `docs/06_tasks/TASK_LEDGER.md` before T-176 rolled the active ledger window.

| ID | Task | Status | Mode | Milestone | Files/Docs | Checks | Notes |
|---|---|---|---|---|---|---|---|
| T-162 | Cancelled request/booking repost flow | completed | Standard | M2 | Customer request Store/View, BookingsView, CustomerTabView, Customer request feature tests, memory docs | RED/GREEN targeted `CustomerRequestsStoreTests`; `git diff --check`; `./scripts/ios-build.sh`; context hygiene | Adds Customer “Create New Request” entry points from cancelled requests and cancelled bookings. The flow pre-fills the existing five-step wizard at Review from the original request, copies cached request photos into the new draft upload path, and publishes through `createRequest` without reusing the original request id. No Supabase schema/RLS/RPC migration. |
