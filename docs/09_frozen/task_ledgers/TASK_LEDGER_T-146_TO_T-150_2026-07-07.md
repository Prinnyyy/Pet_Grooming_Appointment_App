# Archived Task Ledger Rows: T-146 to T-150

These completed rows were moved verbatim out of `docs/06_tasks/TASK_LEDGER.md` during the T-163 docs governance truth cleanup.

| ID | Task | Status | Mode | Files/Docs | Checks | Notes |
|---|---|---|---|---|---|---|
| T-150 | Restore correct task baseline and archive external audit drafts | completed | Quick | root external reports, frozen external-agent archive, ignore/search rules, memory docs | `git diff --check`; `node scripts/context-hygiene-check.mjs` | Confirms `codex/pet-fit-structure-cleanup` is the active baseline after T-149, moves the two non-canonical external reports out of root context, and keeps future root copies ignored. No iOS/Supabase/runtime behavior changed. |
| T-149 | Active design document slimming | completed | Quick | `DESIGN_SYSTEM.md`, `UI_IMPLEMENTATION_NOTES.md`, frozen design-note archives, context hygiene budgets, docs indexes, memory docs | `git diff --check`; `node scripts/context-hygiene-check.mjs`; archive/reference checks | Archives full pre-slim design-system and Groomly UI audit text under `docs/09_frozen/design_notes/`, rewrites active design entrypoints as short current-rule/index files, and adds design-doc word budgets to context hygiene. No iOS/Supabase/runtime behavior changed. |
| T-148 | Pointer/template cleanup and Claude path correction | completed | Quick | removed memory decision pointer, removed lightweight workflow/task templates, frozen template archives, Claude docs, docs indexes, memory docs | `git diff --check`; `node scripts/context-hygiene-check.mjs`; stale-reference search | Archives and removes active compatibility/template Markdown that lost daily workflow value. Keeps Claude docs active because the user still uses Claude, but updates them to current `PRODUCT_BRIEF.md`, `UI_IMPLEMENTATION_NOTES.md`, task, memory, and workflow paths. Detailed removed paths are recorded in `WORKLOG.md` and the reorganization log. No iOS/Supabase/runtime behavior changed. |
| T-147 | Active historical pointer and root brief cleanup | completed | Quick | root Fresh brief archive/removal, Groomly prompt pointer removal, product/design/structure indexes, decision link, memory docs | `git diff --check`; `node scripts/context-hygiene-check.mjs`; active reference search | Archives the original root product/engineering brief under `docs/09_frozen/product_briefs/`, removes the active root file, removes the active Groomly design prompt pointer after its full text was already frozen, and reroutes active docs to `docs/01_product/PRODUCT_BRIEF.md` and `docs/08_design/UI_IMPLEMENTATION_NOTES.md`. No iOS/Supabase/runtime behavior changed. |
| T-146 | AI collaboration context indexing and policy trim | completed | Quick | AGENTS, workflow/context docs, backend policy indexes, feature index, TestOps memory, design prompt pointer, frozen archives, context hygiene script, memory docs | `git diff --check`; `node scripts/context-hygiene-check.mjs`; archive/reference/search-path checks | Implements L0-L4 context access rules, stronger task-stop constraints, compact active policy/index files, frozen pre-trim archives, and one read-only context hygiene command. No iOS source, Supabase schema/RLS/RPC/Storage, migrations, dependencies, simulator, or remote state changed. |
