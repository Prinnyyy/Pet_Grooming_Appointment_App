# Archived Task Ledger Row: T-159

Moved out of `docs/06_tasks/TASK_LEDGER.md` during T-173 to keep the active ledger within the 15-row recovery window.

| ID | Task | Status | Mode | Milestone | Files/Docs | Checks | Notes |
|---|---|---|---|---|---|---|---|
| T-159 | Private RPC lint cleanup | completed | Deep | G0 | `20260707183427_t159_private_rpc_lint_cleanup.sql`, migration test, memory docs | RED/GREEN T-159 migration test; all migration tests; pre-apply migration list/dry-run; authorized `supabase db push --linked`; post-apply migration list/dry-run; RPC metadata SQL; `supabase db lint`; advisors; diff-check; context hygiene | Applied to `lqmasbuqzvcvtawonjlb`. Rebuilds `app_private.accept_groomer_offer(uuid)` and `app_private.complete_booking(uuid)` without unread variables reported by `supabase db lint`. Lint now reports no schema errors; advisors still only report Auth leaked-password protection. |
