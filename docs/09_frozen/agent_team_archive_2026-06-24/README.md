# Agent Team Archive

This archive contains disabled historical Codex role cards and agent definitions moved from `.codex/archive_agents/`.

The active project root keeps only `.codex/config.toml`. The active workflow is the single-agent workflow documented in `docs/05_workflow/SINGLE_AGENT_WORKFLOW.md`.

Do not restore these role cards into `.codex/agents/` unless the user explicitly decides to re-enable multi-agent orchestration.
