# Archived Codex Agents

These role cards and TOML definitions are inactive historical artifacts.

They were moved out of `.codex/agents/` so Codex does not discover them as the default workflow. Do not restore or dispatch them unless the user explicitly re-enables multi-agent orchestration.
