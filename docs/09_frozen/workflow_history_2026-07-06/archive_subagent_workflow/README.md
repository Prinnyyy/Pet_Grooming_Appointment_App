# Archived Subagent Workflow

These files preserve the previous agent-team and subagent workflow for history only.

They are deprecated and must not be used as the default workflow. Do not dispatch archived agents, create per-agent reports, or restore multi-agent orchestration unless the user explicitly re-enables it in a future request.

The active workflow is `../SINGLE_AGENT_WORKFLOW.md`.
