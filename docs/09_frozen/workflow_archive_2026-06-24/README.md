# Workflow Archive

This archive contains superseded workflow material moved out of `docs/05_workflow/`.

## Contents

- `archive_subagent_workflow/`: old subagent workflow docs, templates, and helper scripts.
- `agent_reports/`: historical setup and early task reports.

The active workflow now lives in `docs/05_workflow/SINGLE_AGENT_WORKFLOW.md`.

Do not search this archive by default. Use it only when investigating historical workflow decisions or recovering old context.
