# Pre-Groomly UI Freeze Snapshot

Date: 2026-06-21

This folder preserves the active rule, memory, workflow, and task documents that existed before the Groomly UI phase became the next active workstream.

Use this snapshot only to recover or compare pre-Groomly task context. The active project rules remain in the root files and `docs/`.

Frozen scope at the time of the snapshot:

- T-022 remains completed.
- T-022 post-MVP next-task suggestions are frozen and must not auto-start.
- Groomly UI Slice 1, tracked as T-023, was the only active next implementation task at that time.

Current branch, task numbering, and next-work decisions live in the root `AGENTS.md`, `docs/00_memory/CURRENT_STATE.md`, and `docs/06_tasks/TASK_LEDGER.md`, not in this frozen snapshot.

Excluded:

- `.git/`
- `supabase_api_key`
- build products
- design export binaries
- `.DS_Store`
- SQL reviewed copies
