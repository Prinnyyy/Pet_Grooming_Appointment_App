# T-392 Client Acceptance

<!-- task-artifact
task: T-392
status: completed
type: report
-->

## 验收结论

**2026-09-16（America/Los_Angeles）完成本地模拟验收：60/60 场景通过，80/80 首次分页浏览通过，已知阻断缺陷已修复并复验，测试数据已精确恢复。**

- [执行计划](../../superpowers/plans/2026-09-15-synthetic-multi-simulator-acceptance-plan.md)的场景、分母、时序、性能和安全门槛未降低。
- 私有证据目录：`artifacts/testops/TESTOPS-T392-20260915-A/`；总回执 `final-acceptance.json`，逐例证据 `case-<ID>.json`。历史回执中的“尚待集成/清理”是当时状态，由最终回执收口，不改写旧回执。
- 当前无失败、未执行或工具阻塞的必测场景。历史失败不改记为成功；修复后的独立回执才作为通过依据。
- 本结论是 synthetic 工程验收，不证明真人偏好、真实服务质量或推荐优于其他排序。2026-09-17 用户明确暂不考虑该项独立评审，已移出当前任务清单，不影响本轮工程验收结论。
- 未创建、重置或删除 Auth 账号；未写 Storage；未加入真机、签名、上架或依赖升级范围。

## 场景与数据

| 范围 | 结果 | 主要证据 |
|---|---|---|
| F 发布与会话 | 12/12 | 23 个普通源需求由 UI 发布；R23 是计划允许的窄时间窗 RPC setup。版本关联、宠物快照、幂等恢复和换号隔离通过 |
| Q 报价与选择 | 10/10 | 57 份原始报价意图全部经 UI 操作；非法提案、逐项确认、旧报价撤回和自然到期通过 |
| C 多单与竞争 | 10/10 | 相邻时段、缓冲、每日容量、并发唯一成交、失败端恢复及双方重启读取通过 |
| L 履约闭环 | 8/8 | 客户端取消/改期、实际开始/完成、聊天通知与预约卡路由通过 |
| R 评分与归因 | 10/10 | 独立算术、相关证据、缺项/跨服务隔离、冷启动同桶、唯一评价及丢响应恢复通过 |
| S 规模与恢复 | 10/10 | 双端原生 25+1 分页、7 种全局排序、软变化继续、硬变化拒绝、后台/重启/换号通过 |

冻结源数据：24 需求、57 报价意图、8 非法提案、22 家庭、24 宠物。历史基线是受控导入的 1,228 条服务及评价、整数星级总和 5,791，不是 UI 完成的 1,228 次服务。主样本、历史、竞争分支、冷启动和规模辅助对象分别记录，不能混算。

使用既有 C01-C22 / G01-G26 账号、四个独立模拟器容器。主样本比较在成交/关闭前冻结：276 对评分独立核对、92 个来源排序列表；规模另核对 52 组评分、7 个原生排序及 3 组实际创建时间。规模池为完整授权列表，不靠 run ID 过滤凑数。

数据冻结：anchor `2026-09-15T20:00:00Z`，seed 392；原始 fixture v1 和历史导入前补齐上下文的 v2 均保留。最终 App SHA-256：
`220d1e56fbb2d66842c67d5e279fd7c090d196aa20f1386ab3129f85f5a90ef4`。

## 稳定性与性能

`S-matrix-complete.json`：两角色分别在静态、无关每 10 秒、相关每 60 秒、相关每 10 秒四组中，均为 **10/10 首次完成**；总计 80/80，重复/遗漏为 0。沿用每组五次 1 秒、五次 5 秒页间隔与原固定相位，没有用重试覆盖首次分母。

80 次矩阵是认证 HTTP、每页 6 项共 5 页，不冒充 UI 五页。两端实际 UI 另验证默认 25+1 页；S04-S06 的每类每侧均有首屏与续页之间真正提交变化的交叉证据。对历史评价的受控更新是压力模型，不宣称 App 提供评价编辑功能。

S07：顾客持有可提交确认页时，G11 经 UI 撤回；旧确认被拒绝且 0 预约。随后顾客 UI 取消 S26，美容师旧游标拒绝追加，正常刷新恢复准确的 25 项。顾客端保留 26 条报价历史但仅 25 条可选，撤回项不进入 Top3。

| 最终 26 项读取 | SQL P95 | 认证 HTTP P95 | 首次 HTTP 单列 |
|---|---:|---:|---:|
| 顾客，30 SQL + 30 HTTP | 1152.458 ms | 664.194 ms | 2474.116 ms |
| 美容师，CA-16 后 30 + 30 | 879.246 ms | 379.939 ms | 1683.084 ms |
| 不变预算 | <=1500 ms | <=2500 ms | 不隐藏首读 |

证据：`S-latency-customer.json`、`S-latency-groomer-post-CA16.json`。UI 驱动查找耗时未混入网络延迟。

## 缺陷与处置

下列生产修复均有原路径复验，并纳入最终集成回归。原始 RED/失败产物保留在私有运行目录，逐例回执连接对应 GREEN。

| 编号 | 问题 | 最小修复与验证 |
|---|---|---|
| CA-01 | 退出后并发 token refresh 复活旧会话 | 停止自动刷新、等待已有刷新、合并退出并读取当前会话；真实 SDK 受控传输测试和原生换号通过 |
| CA-03 | 拖动滑块难以精确设定出行半径 | 在原滑块旁补原生 Stepper；10mi 输入和发布预览通过，准入范围不变 |
| CA-05 | 服务端专业证据存在但客户端显示为空 | 接受规范化/受支持的旧信号键，仍拒绝伪造键；实际 12 类证据显示与计数一致 |
| CA-06 | 多行需求说明无法稳定获取焦点 | 显式设置已有 FocusState；原发布路径及后续批次通过 |
| CA-07 | 美容师 Pending Offers 缺少查看需求/撤回入口 | 复用需求详情及现有撤回流程，返回更新本报价；旧顾客确认被权威拒绝 |
| CA-08 | 自然到期报价仍显示 Pending | 展示状态与原始状态分离，保留权威复核；到期项进入历史，其他合法报价仍可选 |
| CA-09 | 评价提交成功但丢响应后无法恢复 | 一次精确身份/会话校验的权威读取，不自动重写；前置失败、已提交恢复、重复拒绝及重启通过 |
| CA-10 | 已评价预约详情出现 SwiftUI 布局循环 | 只将有限详情外层 LazyVStack 改为 VStack；同路径重启读取通过 |
| V-04 | 软证据变化频繁打断浏览 | D-056 有界软证据快照，硬资格/隐私/到期仍实时校验；80/80 矩阵与原生双端通过 |
| CA-11 | 竞争失败的顾客未恢复真实成交方 | 按需求读取唯一权威预约，不重写；三组真实竞争、双方重启及重叠区间核对通过 |
| CA-12 | Bookings 外层滚动出现布局循环 | 只替换有限外层容器，保留行懒加载/分页；原路径双端复验通过 |
| V-05 | 新人同桶排序取舍 | 现行契约稳定；独立分数、同桶和精确并列、15 次首读/6 次分页及原生三模式通过；未改权重 |
| CA-13 | 发布意图退出/重启丢失，变更草稿复用旧操作号 | 请求发送前持久化账号隔离的不可变意图与照片；丢响应后 Close/重启/Retry 仍只产生同一需求 |
| CA-14 | 26 报价触发 SQL 超时 | 单次读取共享实时服务端时区目录，避免重复扫描；准入约 8804ms 降至 363ms，逐项判断/报价集合不变 |
| CA-16 | 静态待评估候选在人工 60 秒边界打断分页 | 证明原乐观区间及四端点不变才延长有效期；保留需评估/确认项与硬门禁；65 秒延续及硬变化负例通过 |

CA-02 是最小恢复演练遇到旧服务资料约束，不是 App 业务缺陷：用户明确授权后，仅对原三行执行一次受锁保护的恢复例外，提交前恢复 guard；全量准备/最终恢复没有关闭触发器。CA-04 是合成街道缺少门牌号，修正夹具而未放宽地址规则。CA-15 名称只见于 UI driver 性能采样：重复读取 AX 标签造成平方级查找；缓存当次标签并等待 Load More 消失，无生产业务改动。

后端追加迁移已部署：`20260915233821`、`20260917011801`、`20260917013719`。最终 111 条历史对齐、dry-run 无待部署项。ACL/RLS 保持，advisors 仅有既存 Auth leaked-password protection 提示，未变更其设置。

## 最终回归

| 验证 | 结果 / 证据 |
|---|---|
| iOS 全量串行集成 | **692 passed、39 skipped、0 failed**；`final-ios-summary.json` / `final-ios-serial.xcresult` |
| iOS 构建 | BUILD SUCCEEDED；`final-ios-build.log` |
| 本地夹具与质量检查 | 47/47；`final-local-fixture-tests.log` |
| Preflight | 169 migration + 10 Edge + 3 brand 通过；`final-preflight.log` |
| 后端最终状态 | `final-migration-list.txt`、`final-db-dry-run.txt`、`final-advisors.json` |

39 个跳过是未配置授权账号的条件化测试，不替代本轮已实际执行的 60 个 UI 场景。xcresult 动态参数调用数与逻辑测试数不同，上表使用顶层摘要，不合并成虚增分母。

## 恢复与安全

`cleanup-exact-restored.json` 及 `cleanup-derived-audit.json` 已通过：

- 清理精确捕获且交叉核实归属的 1,316 需求、1,249 预约、230 新会话、706 新宠物、105 新服务以及相关报价/评价/通知/地址；包含主样本、全部分支、R09、规模与 Giant 服务，不是仅清理 base。
- 恢复 22 顾客、26 美容师的原字段、78 原服务、127 营业时间行和 26 偏好；原 44 宠物、907 通知、49 地址逐表一致。只允许合法 updated_at/eligibility_revision 前进。
- 本轮需求、预约、会话、消息、评价/回答、私有上下文/投影、候选/队列、发布操作与浏览快照残留全部为 0；短期快照通过正常到期清理。
- 配置业务字段相同，合法 privacy revision 不回退；函数/触发器/ACL/RLS 指纹未变。事务先锁定并比对摘要，未发现第三方修改冲突。
- 当次 App/测试进程已停止，DEBUG 一次性故障开关随进程结束；未擦除模拟器或用户资料。

恢复原像来自 base、R09 与 scale 三份准备前记录，而非仅恢复初始 34 个账号。清理提交回执与执行前意图独立保存；不能再次执行已完成的删除事务。

## 执行效率与证据限制

- 主链按角色/账号批量操作，使用 XCTest/accessibility 语义定位。取消常规里程碑截图；图片只用于必要视觉核对或失败诊断。发布、报价、成交等必要 UI 写入未被 API 替代。
- 日常只跑受影响测试；本次修复合并后运行完整回归，未变证据复用。清理和报告收口不再次播种或重跑同一事实。
- 保留早期点击遮挡、键盘未聚焦、picker/继承标识歧义、SQL 类型/断言错误和 Xcode 并行 clone 启动失败。先核对已提交动作，再只续跑缺失动作，没有盲目重放业务写入。
- 首次全量回归的两个测试在授权检查前解包缺失角色，已修正为统一 setup guard；默认并行 clone 启动失败后串行全量通过。旧失败仍在 `final-ios-test.log`，不改记成功。
- `S10-refresh-classified.json` 补齐旧观察：三个 Store start 均对应 27/15/17ms 的 repository cancelled；取消任务被 guard 丢弃，随后同客户端正常刷新 1231ms 成功。不能推断未记录的每次 SwiftUI 手势取消原因；不是失踪业务写入或换号泄漏。
- S07 完成后一次独立后台刷新记录 networkUnavailable，保留旧列表并在下一轮正常读取恢复；没有计入或替换 80 次固定矩阵分母。
- 实际执行通过导出的批次函数、现有 XCTest/原生驱动和逐例审计完成；计划预想的聚合 CLI `run/verify` 仍 fail-closed，**不是已交付的一键重跑工具**。通用 `cleanup` 不覆盖完整分支；本轮使用私有 `cleanup-exact.mjs` 和最终审计，不能调用 base-only 清理代替。没有为统一工具入口扩大工程。
- 收口前长报告保留为私有 `report-before-closeout.md`。Git 交付以当前分支的 `T-392:` 完成提交及远端引用为准：包含本轮依赖的 T-391 工程证据与修复，旧文档归档和 T-394 视频工作流等无关修改保留在工作区，不混入提交。

按 2026-09-17 用户要求，[T-391](T-391_MATCHING_RATING_VALIDATION.md) 的剩余独立标签/盲评及真人决策验证不再安排为下一阶段或当前阻塞；保留历史证据，不自动恢复，也不将未执行的评审标记为通过。
