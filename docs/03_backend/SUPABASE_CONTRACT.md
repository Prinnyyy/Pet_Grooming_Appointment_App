# Supabase Contract

## Contract Status

This backend contract is derived from `Fresh_Pet_Groomer_Marketplace_Engineering_Brief.md`. The T-004 profile/avatar foundation, T-007 atomic profile-onboarding RPC, T-008 pet/photo schema and private bucket, T-010 groomer profile/services/portfolio backend, T-012 grooming request/match backend, T-015 groomer offer backend, T-018 booking/conversation backend, T-020 text-message backend, T-021 completion/review backend, T-044 customer request cancellation backend, T-050 fixed pet data contract, T-049 fixed-service/location/request-photo contract, T-058 groomer availability windows, T-059 groomer full-address/multi-location profile contract, T-060 groomer availability preferences/time-off contract, T-065 private pet-fit SQL taxonomy helpers, T-066 groomer pet-fit claims/portfolio tags, T-067 structured review outcomes, T-068 pet-fit evidence summary, T-069 pet-fit match scoring, T-071 availability enforcement, T-072 availability-aware matching, T-073 claim/portfolio match signals, T-074 customer offer match evidence RLS, T-081A owner-readable evidence dashboard RPC, and T-082 matching fairness calibration are deployed to the fresh Supabase project and mirrored under `supabase/migrations/`. T-050 was deployed after T-075 hardening and backend-validated against the authorized Supabase project. The original project in the account is a legacy project and is not a target for this rebuild.

Once migrations exist, reviewed migrations and verified deployed metadata are authoritative. This document must remain synchronized with them and must never claim a planned object is deployed.

## Environment Boundary

- Remote verification date: 2026-06-26.
- Legacy project: `Prinnyyy's Project`, ref `swdiiyypysyxbnfrxxsv`.
- Legacy project rule: do not inspect, branch, migrate, reset, or mutate; it is not a source for fresh schema or data.
- Fresh project: `Pet Groomer Marketplace`, ref `lqmasbuqzvcvtawonjlb`, organization `Prinnyyy`, region `us-west-1`.
- Project creation: authorized after the user confirmed the reported US$0/month cost; creation returned `ACTIVE_HEALTHY` on 2026-06-19.
- Verification performed: project baseline; migration application; schema, grants, RLS, trigger, function, and Storage inspection; rollback-only policy/RPC tests; security and performance advisors where tool support was available.
- Applied migrations: `20260620105202_t004_profile_foundation`, `20260620105409_t004_optimize_rls_auth_calls`, `20260620172839_t007_create_my_profile`, corrective `20260620180607_t007_fix_create_my_profile_conflict_target`, `20260620192648_t008_pet_data_photo_storage`, `20260620224418_t010_groomer_profile_portfolio_backend`, corrective `20260620225308_t010_merge_groomer_select_policies`, `20260621000444_t012_grooming_request_match_backend`, corrective `20260621002211_t012_fix_create_grooming_request_conflict_target`, corrective `20260621010315_t012_limit_request_photo_snapshot`, `20260621024848_t015_groomer_offer_backend`, `20260621044424_t018_offer_acceptance_booking_backend`, `20260621055915_t020_booking_participant_chat`, `20260621065954_t021_completion_reviews`, corrective `20260621070826_t021_fix_create_review_returning_ambiguity`, `20260622142020_t044_cancel_grooming_request`, `20260623013113_t050_pet_fixed_taxonomy_derived_size`, `20260623065017_t049_request_location_photo_contract`, `20260623223830_t058_groomer_availability_windows`, `20260623233559_t059_groomer_profile_address_location_modes`, `20260624022107_t060_groomer_availability_preferences`, `20260625003519_t065_pet_fit_sql_taxonomy`, `20260625005226_t066_groomer_fit_claims_portfolio_tags`, corrective `20260625010421_t066_fix_claim_tag_trait_checks`, corrective `20260625010716_t066_merge_select_policies_and_index_tag_fk`, `20260625050422_t067_structured_review_outcomes`, `20260625061431_t068_pet_fit_evidence_summary`, corrective `20260625061526_t068_limit_evidence_summary_grants`, `20260625064506_t069_pet_fit_match_scoring`, `20260625073709_t071_availability_enforcement`, `20260625080102_t072_availability_aware_matching`, `20260625081925_t073_claim_portfolio_match_signals`, `20260625085126_t074_customer_offer_match_evidence`, corrective `20260625090429_t074_merge_request_match_select_policies`, `20260626021651_t081a_evidence_dashboard_owner_rpc`, and `20260626033330_t082_matching_fairness_calibration`.
- Local credential file: `supabase_api_key` exists, was not read, is Git-ignored, and has no authorization to appear in iOS code or documentation content.

All Supabase migration and validation operations must target only the task-authorized fresh project, remain separate from the legacy ref, and use Supabase CLI. Remote DDL requires explicit authorization and a reviewed migration. Use `supabase migration new <name>` for new migration files, `supabase db push --linked` for approved remote migration application, `supabase migration list --linked` for migration history, `supabase db query --linked` for focused SQL verification, and `supabase db advisors --linked` for security/performance advisors.

## Platform Boundaries

- Supabase Auth supplies user identity; application role and ownership live in application tables.
- Postgres stores durable product state.
- RLS and explicit Data API grants jointly control client access.
- Storage uses owner/participant-scoped object paths and `storage.objects` policies.
- The iOS app uses a publishable client key only. Secret/service-role keys are forbidden.

## Tables and Roadmap

`profiles`, `customer_profiles`, base `groomer_profiles`, `pets`, `pet_photos`, T-010 groomer profile details, `groomer_services`, `groomer_portfolio_photos`, `groomer_availability_windows`, `groomer_booking_preferences`, `groomer_time_off_windows`, `groomer_fit_claims`, `groomer_portfolio_fit_tags`, `grooming_requests`, `request_matches`, `groomer_offers`, `bookings`, `conversations`, `messages`, `reviews`, and `review_pet_fit_outcomes` are deployed and backend-validated. T-050 fixed pet taxonomy and weight-derived size enforcement, T-065 private pet-fit SQL helper functions, T-068 `groomer_pet_fit_evidence_summary`, T-069 pet-fit match scoring inside `create_grooming_request`, T-071 availability enforcement inside offer/acceptance RPCs, T-072 availability-aware request matching, T-073 low-confidence claim/portfolio match signals, T-074 customer offer match evidence RLS, T-081A `get_my_groomer_pet_fit_evidence_summary` owner aggregate RPC, and T-082 matching fairness calibration inside `create_grooming_request` are deployed and backend-validated. T-070 is iOS-only and deploys no schema.

| Table | Purpose | Key Planned Fields | Access Summary | Roadmap |
|---|---|---|---|---|
| `profiles` | Map Auth user to one app role and shared identity | `id` → Auth user, immutable `role`, `display_name`, private `avatar_path`, timestamps | Owner read/insert; owner updates only display name/avatar path; broader profile presentation deferred | T-004 |
| `customer_profiles` | Customer onboarding role marker and extension point | `user_id`, timestamps | Customer inserts/reads own matching-role row; detail fields deferred | T-004 |
| `groomer_profiles` | Groomer role marker plus marketplace profile details | `user_id`, business name, biography stored as `bio`, 0-5+ experience, base street/city/state/ZIP, 5-50+ radius, canonical `service_location_modes`, legacy synced `service_location_mode`, rating summary, active/verified flags, timestamps | Groomer updates own safe profile fields and active flag; app requires full address plus at least one service-location mode before saving an active profile; authenticated users read active groomer profiles; rating/verification remain server-maintained | T-004, T-010, T-021, T-049, T-059 |
| `pets` | Customer-owned pet profile | `id`, `customer_id`, fixed species/breed/temperament, weight-derived size, birthday, notes, active/delete markers, timestamps | Customer CRUD for owned rows; groomer access only through authorized request snapshot; `size` is trigger-derived from `weight_lbs` | T-008, T-050 |
| `pet_photos` | Metadata for pet image objects | `id`, `pet_id`, `customer_id`, bucket/path, caption/order/primary, timestamp | Customer-managed for owned pet; direct groomer reads not required | T-008 |
| `groomer_services` | Groomer's offered services | `id`, `groomer_id`, fixed `service_type`, title, description, base price, duration, accepted sizes, active flag, timestamps | Groomer manages own; authenticated users read active services for active groomers; request matching filters by active fixed service type | T-010, T-049 |
| `groomer_portfolio_photos` | Metadata for groomer portfolio objects | `id`, `groomer_id`, bucket/path, caption/order, timestamp | Groomer manages own; authenticated users read portfolio metadata for active groomers | T-010 |
| `groomer_fit_claims` | Low-weight self-claimed pet-fit coverage | `id`, groomer, canonical trait type/value, active flag, timestamps | Groomer owner manages own rows; authenticated users read active claims for active groomers plus owners read their own inactive claims; no anon grant; T-073 consumes active matches as low-confidence scoring signals only, and T-082 suppresses their boost when negative earned evidence exists; claims are not expertise proof | T-066, T-073, T-082 |
| `groomer_portfolio_fit_tags` | Optional pet/service/care tags attached to groomer portfolio evidence | `id`, portfolio photo, groomer, canonical trait type/value, timestamps | Groomer owner manages tags for own portfolio photos; authenticated users read tags for active groomers plus owners read their own tags; no anon grant; T-073 consumes matching tags as low-confidence scoring signals only, and T-082 suppresses their boost when negative earned evidence exists | T-066, T-073, T-082 |
| `groomer_availability_windows` | Groomer's weekly availability editor state | `id`, `groomer_id`, ISO weekday, start/end time, enabled flag, timezone, timestamps | Groomer reads and replaces own weekly rows; T-071 uses enabled rows inside controlled offer creation and offer acceptance checks; T-072 also uses them for request matching | T-058, T-071, T-072 |
| `groomer_booking_preferences` | Groomer's availability booking preferences | `groomer_id`, max appointments per day, minimum advance notice days, auto-accept flag, timestamps | Groomer reads and upserts own row; T-071/T-072 enforce max daily appointments and minimum advance notice in offer, acceptance, and request matching paths; auto-accept remains unused | T-060, T-071, T-072 |
| `groomer_time_off_windows` | Groomer's unavailable date windows | `id`, `groomer_id`, title, start/end date, timestamps | Groomer reads/creates/updates/deletes own rows; T-071/T-072 block offer, acceptance, and request matching paths for local dates inside time off | T-060, T-071, T-072 |
| `grooming_requests` | Published customer request with frozen pet/photo snapshot | `id`, customer/pet references, snapshots, fixed service type, notes, preferred range, `location_mode`, street/city/state/ZIP, optional travel range, status/expiry, timestamps | Customer reads own and cancels open/offer states through `cancel_grooming_request`; matched groomer reads only open/offer-eligible assigned requests; direct client writes denied | T-012, T-044, T-049 |
| `request_photos` | Metadata for customer-added request image objects | `id`, `request_id`, `customer_id`, `storage_bucket`, `storage_path`, caption/order, timestamp | Customer inserts/deletes metadata for owned open requests; customers and matched groomers read authorized rows; object paths are protected by matching Storage policies | T-049 |
| `request_matches` | Assignment of an eligible request to a groomer | `id`, request/groomer/customer references, optional score/reason, dismiss reason, status/event timestamps | Groomer reads own match; customer reads backend-generated score/reason only for own matches already linked to visible offers; direct client writes denied; creation and dismissal are controlled | T-012, T-074 |
| `groomer_offers` | Groomer's proposed time, price, and message | `id`, request/match/customer/groomer references, proposal, status/expiry, timestamps | Groomer reads own; customer reads offers for owned request; create/withdraw/status transitions controlled | T-015 |
| `bookings` | Durable result of accepted offer | `id`, request/offer/customer/groomer references, scheduled range/prices/status, completion/cancellation audit fields, timestamps | Participants read own; direct client insert and critical status updates denied; completion/cancellation use RPCs | T-018, T-021 |
| `conversations` | Participant boundary created with booking | `id`, optional request, booking/customer/groomer references, timestamps | Booking participants only | T-018 |
| `messages` | Text-only conversation messages | `id`, `conversation_id`, `sender_id`, `body`, timestamp | Conversation participants read; sender must be a participant; update/delete denied to authenticated clients | T-020 |
| `reviews` | One customer review for a completed booking | `id`, unique `booking_id`, customer/groomer references, rating/content, timestamp | Booking participants read through RLS; booked customer creates once through `create_review`; direct authenticated insert/update/delete denied | T-021 |
| `review_pet_fit_outcomes` | Structured customer review outcomes for pet-fit evidence | `id`, review/booking/customer/groomer references, canonical trait type/value, positive/negative outcome, timestamp | Booking participants read through RLS; rows are created only by `create_review`; direct authenticated insert/update/delete denied; no anon grant | T-067 |
| `favorites` | Name reserved by the Fresh Brief without a defined flow or fields | No contract approved | No access, migration, repository, or UI authorized | deferred |

## Planned Pet-Fit Matching V1

Pet-fit matching v1 is request distribution, not a public groomer directory or direct slot-booking system.

- T-064/T-065 define the shared iOS and SQL trait vocabulary for breed group, size band, care flags, and service fit. T-065 stores the SQL side as private `app_private.pet_fit_*` helper functions only; it does not change request distribution.
- T-066 adds low-weight groomer claims and portfolio tag tables. Claims may later expand eligible context but must not by themselves create "specialist" or "expert" status.
- T-067 extends completed-booking reviews with structured outcomes while preserving the existing rating/content contract. It does not add iOS UI.
- T-068 adds a read-only evidence summary grouped by groomer and trait. Evidence summaries are derived from completed bookings and structured reviews, not manually edited.
- T-069 updates `create_grooming_request` to keep the current hard filters while writing bounded pet-fit evidence adjustments into `request_matches.match_score` and display-ready evidence text into `request_matches.match_reason`.
- T-070 surfaces existing fit reasons in groomer iOS request list/detail UI without adding a new customer groomer-directory screen.
- T-071 enforces availability/time off in offer creation and offer acceptance before any customer self-booking behavior is considered.
- T-072 updates `create_grooming_request` so request matches are created only for groomers whose saved availability fits the requested preferred range.
- T-073 updates `create_grooming_request` so eligible availability-fit matches can receive a small bounded adjustment and reason text from matching active groomer claims and portfolio fit tags. These signals remain low-confidence and do not create eligibility, proof of expertise, specialist status, or direct booking behavior.
- T-082 updates only `create_grooming_request` scoring/reason internals so negative earned evidence is prioritized into the evidence set and suppresses low-confidence claim/portfolio boosts when present. It preserves signature, grants, hard filters, and request lifecycle.
- T-074 lets customers read and display existing match score/reason only after the groomer has submitted an offer for that match. It does not expose unoffered matches, unmatched groomers, or a public directory.
- T-081A adds an owner-readable aggregate RPC for the groomer evidence dashboard. It returns only the authenticated groomer's T-068 aggregate rows and does not broaden request, booking, review, customer, pet, or snapshot table visibility.

## Deployed Views

| View | Purpose | Access Summary | Roadmap |
|---|---|---|---|
| `groomer_pet_fit_evidence_summary` | Derived summary of completed similar bookings, positive/negative structured outcomes, evidence timestamps, and confidence tier by groomer/trait | Read-only `security_invoker`/`security_barrier` view; authenticated and service_role have SELECT only; no anon grant; direct client reads obey underlying table RLS, while controlled backend contexts and the T-081A owner RPC can consume full aggregate evidence | T-068, T-081A |

## Planned Status Values

| Domain | Values | Implementation Note |
|---|---|---|
| Grooming request | `open`, `has_offers`, `booked`, `cancelled`, `expired` | Client cannot freely update status; creation, offer/booking, and customer cancellation transitions follow RPC/controlled mutation rules |
| Request match | `visible`, `viewed`, `dismissed`, `offered`, `hidden`, `expired` | Match creation and system hiding are backend-controlled |
| Groomer offer | `pending`, `accepted_by_customer`, `declined_by_customer`, `withdrawn_by_groomer`, `expired` | Acceptance and competing-offer closure occur atomically |
| Booking MVP | `confirmed`, `completed`, `cancelled_by_customer`, `cancelled_by_groomer` | T-018 writes `confirmed` and cancellation statuses; T-021 writes `completed`; `pending_confirmation`, `in_progress`, `no_show`, and `disputed` are not authorized for MVP behavior |

## Planned Invariants

- One profile role per Auth user; normal UI cannot switch role.
- Grooming request and groomer service types are fixed raw values: `full_groom`, `bath_and_brush`, `haircut_only`, `nail_trim`, `de_shedding`, `custom_request`.
- At most three open requests per customer; a new request defaults to a 48-hour expiry contract.
- Request snapshots do not change when a pet profile changes later.
- Request location mode is fixed to `groomer_comes_to_customer` or `customer_comes_to_groomer`; customer-visits-groomer requests carry a 5-100 mile travel radius, while mobile groomer requests do not.
- Active groomer profiles must include base city/state/radius and at least one canonical `service_location_modes` value. The app also requires street address and ZIP before saving active profiles; the DB keeps those optional for old-row compatibility. Request matching requires compatible service-location mode membership and at least one active groomer service with the requested fixed service type.
- Pet-fit matching v1 preserves request-first distribution. It must not create a customer self-booking path, public all-groomer directory, or client-authored match rows.
- Groomer claimed specialties are low-confidence evidence only. Completed bookings and structured customer reviews are the source of evidence-backed fit summaries.
- Structured review outcomes are created only through `create_review`, use canonical T-065 trait type/value pairs, and store `positive` or `negative` outcomes. They contribute to evidence-backed matching only through the controlled T-069/T-082 `create_grooming_request` scoring path.
- `groomer_pet_fit_evidence_summary` is read-only and derived. It does not store customer, pet, booking, or review detail rows, and it must not be directly mutated by clients.
- `get_my_groomer_pet_fit_evidence_summary()` is the owner dashboard contract for groomers. It is a `SECURITY DEFINER` RPC with empty `search_path`, explicit authenticated non-anonymous groomer-profile validation, `auth.uid()` filtering, no anon execution, and an aggregate-only return signature.
- `request_matches.match_score` and `request_matches.match_reason` remain backend-generated, bounded, and explainable. T-069 combines location fit with completed-booking and structured-review evidence from `groomer_pet_fit_evidence_summary`; T-073 adds a separate low-confidence claim/portfolio signal cap after hard eligibility and availability filters pass; T-082 prioritizes negative earned evidence and suppresses claim/portfolio boosts when negative evidence exists. The client may display results but does not calculate authoritative matching. Customers can read these fields only for matches already linked to offers on their owned requests.
- Groomer availability is stored as one weekly window per ISO weekday for the owning groomer. T-060 also stores owner-only booking preferences and time off windows. T-071 enforces enabled weekly windows, time off, minimum advance notice, max daily appointments, and existing non-cancelled booking conflicts when creating groomer offers and accepting offers. T-072 applies the same availability helper when creating request matches. Customer-facing slot discovery, direct booking, and auto-accept remain deferred.
- Customer request cancellation closes only `open` and `has_offers` requests, declines pending offers, and hides visible/viewed/offered matches.
- At most one match per request/groomer pair.
- At most one active pending offer per request/groomer pair.
- A request and an offer can each create at most one booking.
- Active groomer bookings must not overlap: `start_a < end_b AND start_b < end_a`. Touching boundaries are allowed.
- One conversation is created for the accepted booking flow.
- T-018 booking cancellation does not reopen the original request or any offer; a replacement appointment requires a new request until a future explicit rebooking flow is designed.
- One review per completed booking, written only by its customer.

Exact SQL types, constraints, indexes, cascading behavior, and enum/check implementation are decided and verified in the owning migration task, not invented in this documentation task.

## Deployed Private Helper Functions

| Function | Purpose | Access Summary | Roadmap |
|---|---|---|---|
| `app_private.pet_fit_normalized_text(text)` | Normalizes text inputs for the T-065 pet-fit helpers | `SECURITY INVOKER`, empty `search_path`; `anon`/`authenticated` execute revoked; `service_role` execute granted | T-065 |
| `app_private.pet_fit_breed_group(text)` | Maps breed text to the pet-fit breed group vocabulary (`poodle`, `terrier`) | `SECURITY INVOKER`, empty `search_path`; `anon`/`authenticated` execute revoked; `service_role` execute granted | T-065 |
| `app_private.pet_size_code_for_weight_lbs(numeric)` | Maps stored pet weight in pounds to the persisted `pets.size` code used by request snapshots | `SECURITY INVOKER`, empty `search_path`; `anon`/`authenticated` execute revoked; `service_role` execute granted; `pets` writes derive size through the private trigger rather than direct client execute | T-050 |
| `app_private.set_pet_size_from_weight()` | Trigger function that overwrites `pets.size` from `pets.weight_lbs` before pet inserts and weight/size updates | `SECURITY DEFINER`, empty `search_path`; direct `anon`/`authenticated` execute revoked; called only by the `pets_derive_size_from_weight` trigger | T-050 |
| `app_private.pet_fit_size_band(numeric)` | Maps weight in pounds to the pet-fit size band vocabulary (`XS`, `S`, `M`, `L`, `XL`, `XXL`, `Giant`) independently of stored pet rows | `SECURITY INVOKER`, empty `search_path`; `anon`/`authenticated` execute revoked; `service_role` execute granted | T-065 |
| `app_private.pet_fit_care_flags(text, date, date)` | Derives care flags such as `anxious` and `senior` from request pet fields | `SECURITY INVOKER`, empty `search_path`; `anon`/`authenticated` execute revoked; `service_role` execute granted | T-065 |
| `app_private.pet_fit_service_traits(text, text, date, text, date)` | Derives service-fit traits such as `curly_coat`, `terrier_coat`, `gentle_handling`, and `senior_care` | `SECURITY INVOKER`, empty `search_path`; `anon`/`authenticated` execute revoked; `service_role` execute granted | T-065 |
| `app_private.pet_fit_valid_trait_pair(text, text)` | Validates trait type/value pairs for later claim, tag, and evidence tables | `SECURITY INVOKER`, empty `search_path`; `anon`/`authenticated` execute revoked; `service_role` execute granted | T-065 |
| `app_private.pet_fit_request_traits(text, numeric, text, date, text, date)` | Returns normalized trait rows from request pet fields for later evidence and scoring tasks | `SECURITY INVOKER`, empty `search_path`; `anon`/`authenticated` execute revoked; `service_role` execute granted | T-065 |
| `app_private.pet_fit_traits_from_snapshot(jsonb, text, date)` | Returns normalized trait rows from `grooming_requests.pet_snapshot` JSON for later evidence and scoring tasks | `SECURITY INVOKER`, empty `search_path`; `anon`/`authenticated` execute revoked; `service_role` execute granted | T-065 |
| `app_private.groomer_is_available_for_range(uuid, timestamptz, timestamptz)` | Checks a proposed range against enabled weekly availability, time off, booking preferences, and existing non-cancelled bookings for one groomer | `SECURITY INVOKER`, empty `search_path`; `anon`/`authenticated` execute revoked; `service_role` execute granted | T-071 |

## Deployed RPCs

| Function | Inputs | Result | Verified Contract | Roadmap |
|---|---|---|---|---|
| `create_my_profile` | `p_role user_role`, `p_display_name text` | Caller `id`, authoritative immutable `role`, stored `display_name` | Authenticated non-anonymous caller only; `security invoker`; empty `search_path`; shared profile inserted before matching role marker; same-role retries preserve the first name; different-role retry raises `P0001/profile_role_immutable`; anon has no execute grant | T-007 |
| `create_grooming_request` | `p_pet_id uuid`, `p_service_type text`, optional service notes, preferred range, `p_location_mode text`, street/city/state/ZIP, optional travel radius | Request ID and match count | Authenticated non-anonymous customer only; `security definer`; empty `search_path`; validates role, pet ownership, active pet state, fixed service type, preferred range, location mode/address/state/ZIP/range, and three-open-request limit; freezes pet snapshot and up to 20 pet-photo metadata rows; creates eligible active-groomer matches atomically by compatible `service_location_modes` membership, active fixed service type, location heuristic, and T-072 groomer availability for the preferred range, with legacy single-mode fallback for compatibility; derives request pet-fit traits, uses T-068 aggregate evidence to write bounded evidence-backed match scores/reasons, adds a capped low-confidence T-073 signal from matching active claims and portfolio tags, and T-082 suppresses that low-confidence signal whenever negative earned evidence exists; direct request/match table writes remain denied to authenticated clients | T-012, T-049, T-059, T-069, T-072, T-073, T-082 |
| `cancel_grooming_request` | `p_request_id uuid` | Request ID, request status, cancellation timestamp | Authenticated non-anonymous customer only; `security definer`; empty `search_path`; validates customer role, request ownership, and cancellable status; only `open` and `has_offers` requests transition to `cancelled`; pending offers become `declined_by_customer`; visible/viewed/offered matches become `hidden`; booked/expired requests are rejected | T-044 |
| `dismiss_request_match` | `p_match_id uuid`, optional reason | Match ID, `dismissed` status, dismissed timestamp | Authenticated non-anonymous groomer only; `security definer`; empty `search_path`; validates role, match ownership, dismissible match status, and open/unexpired request state; only the calling groomer's match changes | T-012 |
| `create_groomer_offer` | `p_request_id uuid`, proposed range, price estimate, optional message | Offer ID, offer status, request status | Authenticated non-anonymous groomer only; `security definer`; empty `search_path`; validates role, visible/viewed match ownership, open/unexpired request state, future proposed range, price bounds/scale, message length, no active pending offer, and T-071 groomer availability; unavailable ranges raise `P0001/groomer_unavailable`; creates the offer, marks the match `offered`, and marks the request `has_offers` atomically | T-015, T-071 |
| `withdraw_groomer_offer` | `p_offer_id uuid` | Offer ID, offer status, withdrawn timestamp, request status | Authenticated non-anonymous groomer only; `security definer`; empty `search_path`; validates role, offer ownership, pending/withdrawn state, and open/unexpired request state; withdraws the offer, resets the match to `viewed`, and returns the request to `open` when no pending offers remain | T-015 |
| `accept_groomer_offer` | `p_offer_id uuid` | Booking ID, conversation ID, request ID, offer ID, booking/offer/request statuses | Authenticated non-anonymous customer only; `security definer`; empty `search_path`; validates customer role, offer ownership, pending/unexpired offer, open/unexpired request, offered match, booking uniqueness, confirmed groomer time conflicts, and T-071 groomer availability at acceptance time; stale unavailable offers raise `P0001/booking_conflict`; creates one confirmed booking and one conversation, accepts the selected offer, declines competing pending offers, hides matches, and marks the request `booked` atomically | T-018, T-071 |
| `cancel_booking` | `p_booking_id uuid` | Booking ID, booking status, cancellation timestamp, cancelling user | Authenticated non-anonymous booking participant only; `security definer`; empty `search_path`; validates customer/groomer role from database state, permits only confirmed booking cancellation, returns already-cancelled participant retries idempotently, rejects `completed`, and does not reopen the request or offers | T-018 |
| `complete_booking` | `p_booking_id uuid` | Booking ID, booking status, completed timestamp, completing groomer | Authenticated non-anonymous booked groomer only; `security definer`; empty `search_path`; validates groomer role, booking ownership, and confirmed status; writes `completed_at`/`completed_by`; already completed retries return the authoritative completed state | T-021 |
| `create_review` | `p_booking_id uuid`, `p_rating integer`, `p_content text`, optional `p_pet_fit_outcomes jsonb` defaulting to `[]` | Review ID, booking/customer/groomer IDs, rating/content, created timestamp, updated groomer rating summary | Authenticated non-anonymous booked customer only; `security definer`; empty `search_path`; validates customer role, booking ownership, completed status, rating range, trimmed content length, one-review uniqueness, optional structured outcome array shape, canonical trait pairs, positive/negative outcome values, and duplicate outcome traits; writes `reviews`, optional `review_pet_fit_outcomes`, and groomer rating summary atomically while preserving old three-argument calls | T-021, T-067 |
| `get_my_groomer_pet_fit_evidence_summary` | none | Groomer ID, canonical trait type/value, completed booking count, positive/negative/structured review outcome counts, safe evidence timestamps, and conservative confidence tier | Authenticated non-anonymous groomer only; `security definer`; empty `search_path`; validates caller through `groomer_profiles` plus `profiles.role`; filters to `auth.uid()`; returns only aggregate T-068 evidence rows; customer callers raise `P0001/groomer_profile_required`; no raw customer, pet, request, booking, review, content, or `pet_snapshot` detail is exposed | T-081A |

The corrective T-007 migration changes only the profile insert conflict target to the named `profiles_pkey` constraint. T-012 corrective migrations change the request-match insert conflict target to the named `request_matches_request_groomer_key` constraint and cap request photo snapshots at 20 metadata rows. The T-018 migration installs `btree_gist` and adds a `[scheduled_start, scheduled_end)` exclusion constraint for confirmed groomer bookings so overlaps are rejected while boundary-touching times are allowed. The corrective T-021 migration qualifies the `create_review` inserted-row `RETURNING` columns to avoid PL/pgSQL output-variable ambiguity while preserving the reviewed contract and privileges. The T-044 migration adds only the controlled customer request cancellation RPC. The T-049 migration replaces the request creation RPC signature for fixed service/location fields, adds request photo metadata/storage, and backfills active groomer profiles with a default service-location mode. The T-067 migration replaces `create_review` with a compatible optional structured-outcomes signature and adds `review_pet_fit_outcomes`. The T-068 corrective migration tightens `groomer_pet_fit_evidence_summary` grants to SELECT only for `authenticated` and `service_role`. The T-069 migration replaces only `create_grooming_request` internals to score eligible matches with bounded pet-fit evidence while preserving its signature and grants. The T-071 migration adds a private availability helper and replaces only `create_groomer_offer`/`accept_groomer_offer` internals while preserving their signatures and grants. The T-072 migration replaces only `create_grooming_request` internals to require the T-071 availability helper for match creation while preserving its signature, grants, and T-069 score/reason generation. The T-073 migration replaces only `create_grooming_request` internals to add capped low-confidence claim/portfolio signals after hard eligibility and availability checks while preserving its signature, grants, and evidence-backed scoring. The T-074 migration adds only `request_matches` customer SELECT visibility for matches linked to visible offers and leaves schema, scoring, and RPC signatures unchanged; the corrective T-074 migration merges that customer branch with the existing groomer own-match branch into one authenticated SELECT policy. The T-081A migration adds only the owner-readable aggregate evidence RPC and does not alter table RLS, direct view grants, matching, lifecycle, Storage, or iOS behavior. The T-082 migration replaces only `create_grooming_request` internals to prioritize negative earned evidence and suppress claim/portfolio boosts when negative evidence exists, while preserving the RPC signature, grants, hard filters, lifecycle, RLS, Storage, and iOS behavior.

## Planned RPCs

No additional RPCs are approved beyond the deployed functions above. Pet-fit matching and availability work is expected to replace or extend existing controlled RPCs in owning tasks rather than creating client-side write paths. Future function signatures, return shapes, security mode, grants, and error codes must be finalized in their owning tasks. Cross-record operations must be atomic and explicitly granted only to intended roles.

## Storage Buckets and Roadmap

`avatars` is deployed by T-004 and reused by T-059 for groomer-owned profile photo upload/rendering in the owner profile surface. `pet-photos` is deployed and backend-validated by T-008 and reused by the T-050 Add Pet photo flow. `groomer-portfolio` is deployed and backend-validated by T-010. `request-photos` is deployed and metadata/policy-validated by T-049. Supabase intentionally requires the Storage API for binary deletion; the approved T-009 remote smoke verified actual authenticated upload/delete integration for pet photos and cleaned up all temporary validation data. Every other bucket remains planned until its owning task applies and verifies its Storage contract.

| Bucket | Default Visibility | Path Contract | Roadmap |
|---|---|---|---|
| `avatars` | Private; owner-only in T-004, reused for T-059 owner groomer avatar upload/download; broader customer-facing avatar presentation deferred | `{user_id}/{file_id}.{allowed_image_extension}` | T-004, T-059 |
| `pet-photos` | Private | `{customer_id}/{pet_id}/{file_id}.{jpg,png,heic,heif}` | T-008; T-050 reuses existing bucket |
| `groomer-portfolio` | Private bucket; authenticated object reads for active groomer portfolio metadata; owner-writable | `{groomer_id}/{file_id}.jpg` | T-010 |
| `request-photos` | Private bucket; authorized customer/matched-groomer object reads through metadata-backed policies; customer-owned upload/delete for open requests | `{customer_id}/{request_id}/{file_id}.{jpg,png,heic,heif}` | T-049 |
| `chat-attachments` | Private to conversation participants | `{conversation_id}/{message_id}.jpg` | deferred beyond T-020 |

Object extension is illustrative; implementation tasks must validate allowed MIME types, sizes, generated file IDs, and actual path/metadata consistency. T-020 implements text-only messages and does not deploy a chat attachment bucket.

## Data API Exposure

New Supabase projects may not automatically expose newly created tables to the Data API. Each migration task must explicitly review schema exposure and table/function privileges in addition to enabling RLS. A table with RLS but no required grant is unavailable; a grant without correct RLS is unsafe.

## Client Rules

- Repositories, not SwiftUI views, own queries, RPC calls, and uploads.
- Publishable client keys may be configured in the app; secret/service-role keys may not.
- Client queries remain narrowly scoped even when RLS is present.
- The client does not calculate authoritative matches, accept offers by sequencing direct writes, or locally resolve booking conflicts.
- No runtime fixtures or cached server records act as production fact sources.
